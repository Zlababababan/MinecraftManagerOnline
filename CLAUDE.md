# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-22, phase 12 ouverte — session 1)
- **Fait** : Phases 0–11 (voir docs 03–07) + **phase 12 entamée** (commits 80e38eb, 2ce58a3, e9e84e8). `pnpm check` vert (20 tâches, **400 tests** : protocol 38, shared 110, agent 141 + 1 skip, panel 69, web 45 ; `sampler.test.ts` reste un flake de charge), `pnpm format:check` vert, **e2e Playwright 34/34** (5 projets : `settings.spec.ts` ajouté aux 4 projets de flux). CI GitHub toujours pas observée (aucun remote).
- **Phase 12 — critères 9–11 rejoués en réel (doc 07, note « Ouverture de phase 12 »)** : archive du panel (`build.mjs --panel`) démarrée from scratch sur `172.26.0.1:3100` ; **Windows** : one-liner `install.ps1` complet avec UAC → service shawl **Automatic**, machine online, serveur Vanilla 1.20.1 (copie `D:\mmo-test\servers`) démarré, `Stop-Service` instantané + **Java survivant** + ré-adoption détachée, `-Uninstall` puis réinstallation ; **WSL Ubuntu (systemd)** : `install.sh` système en root (`wsl -u root`), unit `KillMode=process`, serveur sous `mmo` (JDK 21 système), `systemctl stop` → Java survivant → ré-adoption, arrêt propre RCON, `--uninstall --purge`. **Écarts corrigés** : `install.ps1` (fenêtre élevée sous **pwsh 7** : `Get-WmiObject` inexistant → `Invoke-CimMethod` ; `trap` global + transcript `%TEMP%\mmo-install.log` ; `sc config start= auto` (shawl crée en manuel) ; **compte sans mot de passe** → repli explicite `LocalSystem` (erreur 1327) ; diagnostic `Start-Service`) ; **agent ne quittait jamais après SIGTERM** sous systemd (→ `main.ts` `exit()` explicite après `agent.stop()`, `dispose()` `unref()` des tubes) ; **RCON** : vanilla ferme la connexion si commande + junk arrivent dans une même lecture (`if (k != i - 4) return`) → junk envoyé après le premier fragment (doc 06 §5, fake `--rcon-strict-read`) ; **watchdog** : seul `E_TIMEOUT` compte comme gel (un serveur sain a été tué pour « freeze » sous charge à cause des coupures RCON).
- **Phase 12 — revue sécurité doc 03 §6 (note « Revue sécurité »)** : 10 correctifs — **contournement d'auth par pourcent-encodage** (`/%61pi/settings` → `isProtectedRequest` : motif de route + URL décodée), codes d'appairage limités **par adresse** (5/10 min) au lieu du brûlage global, 500 génériques, en-têtes (`http/security.ts` : CSP/X-Frame-Options/nosniff/Referrer-Policy/HSTS si https), `publicUrl` + origine validés (`util/origin.ts`, `requestOrigin()` via `request.protocol/host`), `Object.hasOwn` sur `/api/dist/:platform`, tmp aléatoire ; agent : `files/forbidden.ts` (`ForbiddenRoots` : état + installation jamais serveur/destination, ni répertoire surveillé/scan contenu), `migration.finalize` borné au chemin enregistré, manifestes backup locaux, chemins réservés (`isReservedPath` : corbeille casse ignorée + marqueur) sur write/mkdir/rename/copy/delete/upload/fetch, noms réservés Windows + `foo.`. Limites acceptées (panel = autorité : javaPath/launch.jar/runtime.update non signé) et **dette** (`/ws/probe` sans borne, `/api/setup` sans verrou, login /64 IPv6, bornes de taille `fs.fetch`/upload/extractions, SSRF `fs.fetch`, ReDoS `logs.search`, `\n` dans `config.set` live) listées dans doc 03 §6.
- **Phase 12 — e2e** : `apps/web/e2e/settings.spec.ts` (thèmes via `theme-menu`/`theme-{light,dark,auto}` + `emulateMedia`, persistance `/api/auth/me` ; notifications : le centre est une **vue filtrée des événements par les préférences** → marquer lu *après* activation de `pref-server.state`, entrée = `button[data-testid^="notification-"]` (libellé type + état + nom) ; Réglages : `settings-public-url`, `access-test-run` → `access-test-result[data-ok=true]` via `/ws/probe` réel, distribution factice `0.0.1-e2e` déposée par `PUT /api/admin/dist/files/:file` + `manifest` → carte/one-liners/`/dist` + `x-sha256`/`install.ps1` injecté → `distribution-clear` (dialog)).
- **Environnement de test laissé en place (pour le reboot Windows)** : panel de test = archive extraite `D:\mmo-test\mmo-panel` (scripts d'installation patchés), données `D:\mmo-test\panel-data`, lancé par `D:\mmo-test\start-panel.cmd` (`172.26.0.1:3100`, admin / `Mmo-Test-2026!`, `D:\mmo-test\cookies.txt` + client `api.mjs` du scratchpad) ; agent Windows installé (service `mmo-agent` Automatic, LocalSystem, bundle **corrigé** copié dans `versions/0.11.0/agent.js` → actif au prochain démarrage du service), machine `PC-Windows`, serveur `vanilla-1.20.1` (desiredState running, `agents.restoreOnBoot=true`). Désinstallation : one-liner `-Uninstall -Purge`. WSL : tout désinstallé (compte `mmo` conservé, `/srv/minecraft` reste).
- **Pièges résolus (à ne pas réintroduire)** : (1)–(41) phases 5–11. (42) Sous pwsh 7, `Get-WmiObject` n'existe pas → CIM ; une exception dans une fenêtre élevée la ferme sans message → `trap` + transcript. (43) `shawl add` crée le service en démarrage **manuel**. (44) Compte Windows sans mot de passe : aucun service ne peut ouvrir de session sous ce compte (1327/7038). (45) Node : `process.exitCode` ne suffit pas si un handle (tubes d'un enfant détaché) reste référencé → `process.exit` explicite après vidage de stdout. (46) Vanilla RCON : jamais deux paquets dans une même écriture/lecture. (47) Fastify : le routeur décode le pourcent-encodage avant de router ; un hook qui teste `request.url` brute est contournable → tester `routeOptions.url`. (48) La VM WSL s'arrête ~8 s après la dernière session → garder un `wsl -e sleep` en arrière-plan pendant un test de service ; `wsl -u root` évite le mot de passe sudo. (49) PowerShell : les variables sont insensibles à la casse (`$S` et `$s` sont la même). (50) Git Bash convertit les arguments `/api/...` en chemins Windows → passer par PowerShell pour un client HTTP en ligne de commande. (51) Le centre de notifications filtre les événements par préférences **au moment de la lecture** : activer une préférence rend visibles des événements passés.
- **Dette / non fait** : critères restants à la main : **reboot Windows** (procédure ci-dessous), relais `sudo` d'`install.sh` en terminal interactif, Linux ARM réel, launchd macOS, migration Windows → Linux ARM, push Android/iOS, accès hors réseau ; dette sécurité doc 03 §6 ; `release/0.11.0` **périmé** (bundle sans les correctifs de cette session → rebuild avant toute publication) ; panel packagé pour l'hôte de build seulement ; clé de release non générée ; `runtime.update` réel jamais exercé ; dette phases 9–10 inchangée.
- **Prochaine étape (phase 12, suite)** : test d'échelle (~56 serveurs détectés sur copies, 3–4 fake simultanés, métriques 48 h accélérées), purges/rétentions, sauvegarde/restauration du panel, accessibilité de base, polish UI, dette sécurité (au moins `/ws/probe` borné, verrou `/api/setup`, bornes de taille `fs.fetch`/upload), rebuild de la release + smoke, génération de la clé de release, tag `v1.0.0`.
