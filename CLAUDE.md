# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-22, fin de session phase 11)
- **Fait** : Phases 0–10 (voir docs 03–07) + **11** (packaging et installation : archives des 4 plateformes + archive du panel, scripts d'installation servis par le panel, services, pipeline de release signé, guide utilisateur). `pnpm check` vert (20 tâches, **398 tests** : protocol 38, shared 110, agent 136 + 1 skip, panel 69, web 45 ; `sampler.test.ts` reste un flake de charge sous turbo), `pnpm format:check` vert, **e2e Playwright 22/22** (inchangés). **CI GitHub toujours pas observée** (aucun remote) — les nouveaux jobs (smoke par OS, `release-archives`, workflow `release.yml`) sont écrits à l'aveugle. Commits phase 11 : voir `git log` (protocole+agent+outillage, panel, web, docs).
- **Phase 11 — outillage `tools/release/`** (pur Node, sans python, pas de dépendance) : `archive.mjs` (lecteur/écrivain tar(.gz) ustar+pax et zip deflate, reproductibles : entrées triées, mtime `SOURCE_DATE_EPOCH`), `constants.mjs` (`NODE_VERSION` = `.node-version`, sha256 des 4 distributions Node officielles, shawl 1.9.0 sha256, `hostPlatform()`), `download.mjs` (cache `tools/release/.cache/` ignoré), `build.mjs` (`--platforms`, `--out` (défaut `release/`, ignoré), `--key`/`MMO_SIGNING_KEY`, `--release` refuse la clé dev et exige la clé publique dans `keys.ts`, `--skip-build`, `--panel`), `smoke.mjs` (sha256/contenu/modes + `launcher --version` réel sur l'hôte, `--host`), `publish.mjs` (`--panel --user [--password|MMO_PANEL_PASSWORD]` via API, ou `--dist-dir` copie locale), `README.md`. Sorties `release/<v>/` : `agent-<v>.js`, `mmo-agent-<v>-{win-x64.zip,linux-x64,linux-arm64,darwin-arm64 .tar.gz}` (~35–45 Mo ; disposition launcher : `launcher.cjs`, `versions/<v>/agent.js`, `current.json`, `runtime/<node>/{node.exe|bin/node,LICENSE}`, `runtime-current.json`, `manifest.json`, `install.ps1/.sh`, `README.txt`, `shawl.exe` win), `manifest.json` (`DistManifest` : version, protocolVersion, runtimeVersion, builtAt, signingKey `dev`|SPKI, bundle {file,sha256,size,signature}, platforms), et avec `--panel` : `mmo-panel-<v>-<hôte>.zip|tar.gz` (218 Mo win-x64 : `runtime/`, `app/` = `pnpm deploy --prod --legacy --config.node-linker=hoisted` filtré à dist/node_modules/drizzle/install/package.json, `web/`, `dist-agent/` (les archives agent + manifeste + bundle), `mmo-panel.cmd`/`.sh` posant `MMO_WEB_DIR`/`MMO_DIST_DIR`/`MMO_DATA_DIR`, `shawl.exe` win) + `panel-<hôte>.json`. Scripts racine `release:build/smoke/publish`. Reproductibilité vérifiée (sha256 identiques sur 2 builds). **Attention** : `pnpm deploy --prod` laisse pnpm croire à un état « production » → un `pnpm build` suivant tente `install --production` (abandonné sans TTY) ; `build.mjs --panel` fait le deploy **après** le build, et `pnpm install --frozen-lockfile` remet d'aplomb.
- **Phase 11 — protocole client (par ajout)** : `DIST_PLATFORMS`, `distPlatformSchema`, `distManifestSchema`/`DistManifest`, `distArtifactDtoSchema`, `distStatusDtoSchema` (`available, version, protocolVersion, runtimeVersion, builtAt, signingKey, releasePublished, platforms{p→{file,sha256,size,url}}, install{windows,unix}|null`), `distPlatformDtoSchema` (JSON plat lu par les scripts). Aucun changement de protocole agent.
- **Phase 11 — agent 0.11.0** : commande **`pair --panel <ws(s)://…/ws/agent> --pair-code <code> [--state-dir]`** (`runPair` → `Agent.pair()` → `AgentConnection.pairOnly()` : une connexion, `pair.request`, état écrit, fermeture ; idempotent si déjà appairé ; exit 0/1/2), utilisée par les installeurs avant d'enregistrer le service. **Launcher** : `ONE_SHOT` (`--version`, ou `pair`/`scan` en argv[2]) = spawn unique stdio inherit, env `MMO_AGENT_HOME`/`MMO_AGENT_VERSION`, code de sortie rendu tel quel, pas de relance ni d'essai (seule évolution du launcher « figé »). Tests : `connection.test.ts` (pairOnly), `launcher.test.ts` (one-shot exit 3).
- **Phase 11 — panel 0.11.0** : `config.distDir` (`MMO_DIST_DIR`, défaut `<dataDir>/dist`). `services/distribution.ts` (`DistributionService`) : `manifest()` (cache par mtime), `status()`, `platform(p)`, `filePath(file)` (fichiers du manifeste seulement), `installScript(name, requestOrigin)` (templates `apps/panel/install/`, `DEFAULT_INSTALL_DIR` relatif à `src/`/`dist/`, `__PANEL_URL__` ← `panel.publicUrl` sinon origine), `putFile` (tmp + rename), `putManifest` (sha256/tailles vérifiés → `E_VALIDATION`/`E_CHECKSUM_MISMATCH`, puis `releases.publish` si version absente), `syncRelease()` (au `buildApp` : manifeste présent → release d'agent publiée — cas archive du panel), `clear()`. Routes `http/routes/phase11.ts` : `GET /install.ps1|/install.sh` (public, text/plain utf-8, no-cache ; `requestOrigin()` honore `x-forwarded-proto/host`), `GET /dist/:file` (public, content-length, `x-sha256`, attachment), `GET /api/dist`, `GET /api/dist/:platform` (publics), `PUT /api/admin/dist/files/:file` (octet-stream, 512 Mo), `PUT /api/admin/dist/manifest`, `DELETE /api/admin/dist` (admin, audit `distribution.published/cleared`). Test `distribution.test.ts` (scripts + URL injectée, vide, dépôt complet, sync au démarrage). `install.sh` (POSIX sh, testé sous dash/WSL) : options `--pair-code --panel --archive --dir --state-dir --user --user-service --no-service --uninstall --purge` ; mode système (`/opt/mmo-agent`, `/var/lib/mmo-agent`, user `mmo` créé, unit `KillMode=process`, auto-`sudo` avec retéléchargement du script depuis `$PANEL/install.sh` car `$0` = sh en pipe) ou utilisateur (`~/.local/share/mmo-agent{,/app}`, `systemctl --user`, `loginctl enable-linger`) ; macOS LaunchDaemon/LaunchAgent `AbandonProcessGroup` ; `json_field()` = sed sur JSON plat (pas de jq) ; `versions/` conservé à la réinstallation ; appairage via `agent.js pair` sous le compte de service (`runuser`/`sudo -u`, `HOME=$STATE_DIR`). `install.ps1` (PS 5.1+) : `-PairCode -Panel -Archive -InstallDir -StateDir -ServiceAccount User|LocalSystem -NoService -Uninstall -Purge -Elevated` ; auto-élévation = copie de `$MyInvocation.MyCommand.ScriptBlock.ToString()` dans `%TEMP%\mmo-install.ps1` + `Start-Process -Verb RunAs -Wait` ; `Invoke-RestMethod /api/dist/win-x64`, `Get-FileHash`, `Expand-Archive` ; service `shawl add --restart --stop-timeout 60000`, `sc failure`, compte utilisateur via `Get-Credential` + `SeServiceLogonRight` (secedit export/import) + `Win32_Service.Change` (WMI, pas de mot de passe en ligne de commande).
- **Phase 11 — web** : `api/phase11.ts` (`useDistribution`, `useClearDistribution`, `phase11Keys.dist`), `components/admin/DistributionCard.tsx` (Réglages : `distribution-card`, `distribution-empty` (commandes build/publish), `distribution-version`, `distribution-release`, `distribution-dev-key`, lignes `dist-platform-<p>[data-available]` avec lien de téléchargement, `dist-oneliner-windows/unix`, `distribution-clear` avec `window.confirm`), i18n `web:distribution.*` fr/en (plateformes en `win_x64`… via `tDynamic`). Test `admin/distribution.test.tsx`.
- **Phase 11 — docs** : `docs/guide/installation.md` (panel : archive, lancement, wizard, service shawl/systemd/launchd, mise à jour ; agents : one-liner, détail par OS, reboot, hors ligne), `docs/guide/ajouter-une-machine.md`, `docs/guide/faq-reseau.md` (tailscale serve, direct IPv6 + DNS-01 + pinholes, manuel, FAQ) ; doc 03 §3 et doc 07 amendés ; `tools/release/README.md`, `tools/signing/README.md`, `keys.ts` (procédure de rotation) ; README (liens guide + pipeline). CI : étape smoke par OS dans `check`, job `release-archives` (ubuntu, 4 archives, artefact 7 j), `release.yml` (tag `v*`, secret `MMO_SIGNING_KEY_PEM`, `--release`, publication GitHub).
- **Validé en réel cette session** : archives des 4 plateformes (+ reproductibilité, `tar`/`Expand-Archive` système, `launcher --version` Windows et WSL) ; panel local + `publish.mjs` API ; `install.sh --user-service` de bout en bout dans WSL Ubuntu 22.04 (systemd --user actif et authentifié, linger, machine `online`, `--uninstall`) ; `install.ps1 -NoService` sous Windows PowerShell 5.1 (appairage réel, `-Uninstall -Purge`) ; archive panel win-x64 démarrée from scratch (front, `/api/dist`, `install.ps1`).
- **Pièges résolus (à ne pas réintroduire)** : (1)–(35) phases 5–10 (CLAUDE.md précédent / docs). (36) Un motif `.gitignore` `release/` non ancré ignore aussi `tools/release/` → écrire `/release/`. (37) `pnpm deploy --prod` (même `--legacy`) bascule l'état pnpm du workspace en « production » : le `pnpm build` suivant déclenche un `install --production` (abandonné sans TTY, mais il casse le script) — déployer après le build, puis `pnpm install --frozen-lockfile`. (38) Le zip de Node et le tarball utilisent un préfixe `node-v<v>-<plat>/` ; le binaire est `node.exe` (win) ou `bin/node` (unix) — `launcher.nodeOf()` cherche exactement ces chemins sous `runtime/<v>/`. (39) `curl | sh` : `$0` vaut `sh`, pas le script → pour s'auto-relancer en `sudo`, retélécharger le script. (40) Les tests qui figent la version d'agent (`agent.test.ts`, `panel-agent.test.ts`, `phase9.test.ts` `previous`) doivent être mis à jour à chaque bump ; `agent.test.ts` utilise maintenant `AGENT_VERSION`. (41) Sous Windows, `pkill` n'existe pas et un dossier où un shell Bash a fait `cd` est « busy » pour `rm -rf`.
- **Dette / non fait (volontaire ou bloqué)** : critère phase 11 **partiellement rejoué** — reste à jouer à la main (pas d'élévation ni de machine vierge dans la session) : service shawl sous compte utilisateur + reboot Windows, installation Linux système (`sudo`) et Linux ARM réel, launchd macOS, vérification par OS que les serveurs survivent à l'arrêt du service ; critères phases 9–10 toujours non rejoués (migration Windows → Linux ARM, push Android/iOS, accès hors réseau) ; clé de release non générée (procédure documentée, `--release` bloque la clé dev) ; panel packagé pour l'hôte de build uniquement (modules natifs) et sans script de service (doc manuelle) ; pas d'e2e Playwright phase 11 ; `runtime.update` réel jamais exercé (le launcher swap est testé) ; dette phases 9–10 inchangée (onglet admin utilisateurs/sauvegardes, IPv6 temporaires, DNS manuel sans renouvellement, bundle web ~900 kB…).
- **Rappels phases 2–10** : protocole jalons A/B/C + phase 9 (migration, java.install, agent.update signé + launcher rollback) + phase 10 (notifications, accès) ; shared (i18n typée, Java manifest, parsing logs, détection, TPS, cron, codecs, textes push) ; agent (connexion/backoff, lancement détaché, RCON, ré-adoption, `fs.*` jailé, config, joueurs, logs, métriques, watchdog, `TaskRunner`, backups, transferts, migration, Java installer, updater, `networkAddresses`) ; panel (Fastify 5 + Zod, Drizzle, sessions + RBAC, wizard, appairage, `/ws/agent` + `/ws/client`, adoption, relais, métriques, tasks/backups/planificateur/transferts, `VACUUM INTO`, migrations, releases, Java géré, Web Push RFC 8291/8292, `NotificationsService`, `AccessService` ACME DNS-01/HTTPS à chaud/DynDNS) ; web (Vite + React 19 + Mantine 8 + TanStack + Zustand + xterm + i18next + PWA ; centre de notifications, Réglages admin, cartes accès). Détails : notes « Implémentation » docs 03–07.
- **Environnement** : `node` global 22.14 (pnpm/vitest/Playwright ; **zstd absent ⇒ repli gzip en test**), Node 24.19 via `%LOCALAPPDATA%\pnpm\bin\node.EXE` ; Chromium Playwright installé ; PowerShell 5.1 et 7 ; **WSL Ubuntu 22.04 avec systemd** (sudo à mot de passe ; joignable depuis Windows via `172.26.0.1` côté Windows — le panel de test doit écouter sur cette adresse) ; shell non élevé (pas de service Windows installable sans UAC) ; pas de Docker. `tools/release/.cache/` contient les distributions Node/shawl téléchargées (~170 Mo). `apps/web/tsconfig.json` inclut `e2e` + `playwright.config.ts` (⇒ `apps/panel` typé avec lib DOM et i18next typé). `.prettierignore` : `docs/*.md` seulement (les `docs/guide/*.md` sont formatés).
- **Prochaine étape** : **Phase 12 = durcissement et release 1.0** (doc 07) : d'abord **rejouer à la main** les critères restants des phases 9–11 avec le guide (`docs/guide/installation.md`) — sur ce PC : `install.ps1` complet en PowerShell admin (service shawl sous compte utilisateur, reboot, serveurs survivants), dans WSL : `install.sh` système avec `sudo` ; puis, si matériel dispo, Linux ARM, migration Windows → Linux ARM, push Android/iOS, accès hors réseau tailscale puis direct — et noter les écarts dans doc 07. Ensuite : e2e complets fr/en desktop/mobile/thèmes (+ e2e phase 10–11 si la fixture le permet), revue sécurité doc 03 §6 (+ audit chemins jailés, routes publiques `/dist`, `/install.*`, `/ws/probe`), test d'échelle ~56 serveurs / 3–4 fake simultanés / métriques 48 h, purges/rétentions, sauvegarde/restauration du panel, accessibilité, polish UI, génération de la clé de release + tag `v1.0.0`.
