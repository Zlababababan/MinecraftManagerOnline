# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-22, fin de session phase 4)
- **Fait** : Phases 0 (spikes), 1 (monorepo), 2 (`protocol` + `shared`), 3 (`apps/agent`), **4** (`apps/panel` noyau). `pnpm check` vert (20 tâches, 205 tests : protocol 30, shared 92, agent 66, panel 16, web 1), `pnpm format:check` vert. **CI GitHub toujours pas observée** (aucun remote).
- **Phase 4 — panel** (`apps/panel`, `PANEL_VERSION = 0.4.0`) : Fastify 5 + `fastify-type-provider-zod` + `@fastify/websocket` + `@fastify/cookie` ; Drizzle + `better-sqlite3` 13 (natif, **compilé par MSBuild sous Windows** car prebuild absent ; `allowBuilds` dans `pnpm-workspace.yaml`) ; `@node-rs/argon2`. Structure : `config.ts` (env `MMO_*`, refus `0.0.0.0`/`::`), `db/` (`schema.ts` doc 04 + amendements, `schema-metrics.ts`, `client.ts` PRAGMAs + migrations rejouées à l'ouverture ; migrations SQL commitées `drizzle/{mmo,metrics}` via `pnpm db:generate[:metrics]` — **`COLLATE NOCASE` et `WITHOUT ROWID` posés à la main dans le SQL**, à reporter si on régénère), `services/` (settings, audit, `EventBus` persistant + diffusion, users/RBAC, sessions, machines/appairage/rotation, `ServersService` = adoption/conflits/réconciliation/états/joueurs/config agent, `JavaResolver` manifest Mojang, `ProcessedEventsService` dédup), `agents/` (`AgentSession` = handlers `pair.request`/`auth.hello`/`sync.state` + événements + watchdog heartbeat + ack batché + `pushConfig` sérialisé/coalescé, `AgentRegistry` 1 session/machine, `ConsoleRelay` ring 1000 lignes, `ws-transport`), `clients/hub.ts` (`/ws/client`, abonnements par canal), `http/` (`auth.ts` cookie + refus par défaut + `config.role`, `errors.ts` → `ApiError`, routes `setup-auth`/`users`/`machines`/`servers`/`misc`/`ws`), `context.ts`, `app.ts` (`buildApp` + maintenance horaire), `main.ts`.
- **Contrat panel↔front** : `@mmo/protocol/client` (nouveau sous-chemin) = DTO REST (`userDto`, `machineDto`, `serverDto`, `eventDto`, conflits…), schémas de requêtes, `PANEL_ERROR_CODES` (`E_FORBIDDEN`, `E_RATE_LIMITED`, `E_SETUP_REQUIRED`, `E_SETUP_DONE`, `E_AGENT_OFFLINE`, `E_VALIDATION` — chaînes fr/en ajoutées à `errors` de shared + test de parité), messages `/ws/client` (`hello`, `event`, `server.state`, `machine.heartbeat`, `console.snapshot`/`console.lines`, `subscribe`/`unsubscribe`/`ping`). **La phase 5 consomme ce module tel quel.**
- **API REST** (préfixe `/api`, sessions cookie `mmo_session`, rôles `viewer` < `operator` < `admin`) : `health` · `setup/status`, `setup` (wizard, verrouillé ensuite, VAPID généré) · `auth/login|logout|me` (+ `PATCH me`) · `users` CRUD (admin ; dernier admin protégé) · `machines` (création = machine `pending` + code d'appairage + one-liners si `panel.publicUrl`, `pairing-codes`, `rotate-secret`, `info`, `directories`, `scan` → adoption) · `servers` (liste/détail, `POST` = ajout manuel par `scan.run { paths }`, `PATCH`, `DELETE`, `start|stop|restart|kill`, `eula-accept`, `command`/`rcon` + `command-history`, `console?sinceSeq`, `players`, `conflicts` + `conflicts/resolve` copy/migrate/ignore) · `events?sinceId&serverId&type` · `audit` · `settings` (clés éditables whitelistées).
- **Décisions actées en phase 4** (docs amendées) : adoption **automatique** des détections (ID ULID panel ; ID du marqueur conservé s'il est inconnu = base restaurée ; conflit explicite si connu ailleurs) — doc 04 §3 ; `pairing_codes.machine_id` renseigné à la création, 5 échecs brûlent les codes actifs — doc 04 §2 + doc 05 §3 ; colonnes `machines.agent_token_prev_hash/_until`, `servers.detection_json`, table `processed_events` — doc 04 ; `auth.ok.subscriptions` = consoles regardées avec `sinceSeq`, 1 session/machine (4000), offline fermé par le panel (4002), révocation (4003) — doc 05 §4/§5 ; réconciliation + `server.start` des `desired_state=running` après `sync.state` — doc 05 §4 ; répertoire data par env, pas par wizard — doc 03 §8 ; cookie/RBAC/rate-limit — doc 03 §6.
- **Tests panel** : `http/api.test.ts` (migrations from scratch, wizard, login/logout/expiration/rate-limit, RBAC, dernier admin, machines/codes, événements/réglages), `agents/session.test.ts` (faux agent `ws` : appairage/essais/TTL/usage unique, auth/négociation N+1/N-1/`E_INVALID_PAYLOAD`, une session par machine, heartbeat→offline, réconciliation `sync.state`, dédup + ack des critiques, crash, rotation/révocation, adoption/conflits copy+migrate/removed, `/ws/client` console avec rattrapage), `integration/panel-agent.test.ts` (**agent réel importé depuis `apps/agent/src` + fake Java server** : appairage → scan → adoption + marqueur → start → console live → commande → coupure/reconnexion avec rattrapage `seq` sans doublon → RCON/joueurs → stop → agent hors ligne = `E_AGENT_OFFLINE`). Smoke manuel : `dist/main.js` sous Node 24 sur un vrai `data/`.
- **Rappels phases 2–3** : protocole jalon A (33 req + 11 events, `RpcPeer`, négociation N/N-1, fixtures de contrat) ; shared (i18n TS typée, Java manifest→table, parsing logs, détection 22/22 fixtures) ; agent 0.3.0 (connexion/backoff, lancement détaché, RCON maison, console ring 5000, ré-adoption PID+heure+cmdline, `scan.run`, `agent.configure.servers`, fake Java server). Détails d'implémentation : notes « Implémentation » dans docs 03–06.
- **Dette / non fait (volontaire)** : `metrics.sample` ignoré (phase 7) ; push web (`web-push`) non branché (clés VAPID prêtes) ; `fs.*`/`config.*`/`logs.*` non relayés par l'API (phase 6) ; tasks/backups/transferts (phases 7/8) ; `agent.update`/install scripts `install.ps1|sh` servis par le panel (phase 11 — les one-liners sont déjà formés) ; `rcon_password_enc` inutilisé (l'agent gère RCON localement) ; purge `processed_events`/events/audit = job horaire simple ; `VACUUM INTO` non planifié.
- **Environnement** : `node` global 22.14 (scripts pnpm/vitest tournent dessus), Node 24.19 via `%LOCALAPPDATA%\pnpm\bin\node.EXE` ; `better-sqlite3` compilé pour l'ABI du Node qui a installé (relancer `pnpm rebuild better-sqlite3` si on change de Node et que le chargement échoue). `apps/panel/tsconfig.json` sans `rootDir` (le test d'intégration importe `../../../agent/src`), `tsconfig.build.json` exclut `src/test/**` et `*.test.ts`. `.prettierignore` : `apps/panel/drizzle/**`.
- **Prochaine étape** : **Phase 5 = front PWA socle** (doc 07) : Vite + React 19 + Mantine 8 + TanStack Router/Query + Zustand + xterm + i18next (`resources` de `@mmo/shared`) + vite-plugin-pwa ; consommer `@mmo/protocol/client` (DTO, messages `/ws/client`) ; pages : wizard first-run, login, dashboard (machines + cartes serveurs), page serveur avec console xterm (abonnement `console:<id>`, snapshot + lines, envoi `POST /api/servers/:id/command`), machines (ajout + code/one-liners). Servir le build par le panel (`@fastify/static`, fallback SPA) ; e2e Playwright desktop + mobile, fr + en.
