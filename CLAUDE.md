# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-23, phase 12 — session 2, tag v1.0.0 posé)
- **Fait** : Phases 0–12 complètes côté code — **tag `v1.0.0`** (commit 5f19089). `pnpm check` vert (20 tâches, **411 tests** : protocol 38, shared 110, agent 147, panel 77, web 45 ; `sampler.test.ts` reste un flake de charge, vert isolément), `format:check` vert, **e2e Playwright 42/42** (34 + a11y ×4 + sauvegarde panel ×4). CI GitHub toujours pas observée (aucun remote). **Résultat du reboot Windows : non rapporté par l'utilisateur** (le prompt de relais portait « [à compléter] »).
- **Session 2 (commits 44e5b12 → 5f19089)** : (1) **dette sécurité** (doc 03 §6 « Dette traitée ») — `/ws/probe` borné (8 connexions, frame 256 Kio, 4 Mio cumulés), `/api/setup` verrou + 5/min par IP en `preValidation`, bornes `fs.fetch` 8 Gio + http(s) seul / upload 64 Gio + taille déclarée / extractions tar-zip 64 Gio + 2 M entrées (zip : compteur sur flux inflaté), code `E_TOO_LARGE` (413) ; **régression corrigée** : `migration.finalize` échouait en E_NOT_FOUND (avalé → dossier jamais renommé) car le panel retire le serveur de la source avant finalize → chemin exporté mémorisé par migrationId. (2) **Échelle** (`panel/integration/scale.test.ts`) : 56 serveurs (fixtures) détectés ~0,5 s par agent réel, 4 fakes simultanés + métriques, 48 h × 56 ingérées en 6,5 s, requêtes < 10 ms ; **bug corrigé** : allocation RCON non sérialisée (starts simultanés → même port). Miroir réel `D:\mmo-test\scale` (17 Go) : 53/55 détectés en 8 s via `mmo-agent scan`. (3) **Sauvegarde/restauration du panel** : `mmo-panel restore <fichier>` (`restorePanelBackup` : integrity_check, refus si -wal non vide, base mise de côté), carte « Sauvegardes du panel » dans Réglages (les hooks n'avaient pas d'UI), test e2e intégration (sauvegarde → dérive → restauration → agent reconnecté, serveur même ID) + purges sur horloge simulée ; guide §1.6, doc 04 §8.4. (4) **Accessibilité** : `e2e/a11y.spec.ts` (axe-core WCAG 2.1 AA, 7 pages × clair/sombre × 4 projets) zéro violation serious/critical après correctifs (primaryShade 9 + autoContrast + teal/green-9 assombris, dimmed/placeholder relevés, Popover.Target sur le bouton, aria-label Progress/SVG, CloseButton nommés via useT dans App). (5) **Release 1.0.0** : clé mainteneur générée → `C:\Users\Yassin\.mmo\signing\mmo-release.private.pem` (publique dans `RELEASE_KEYS`), builds `--release` retirent la clé de dev (`MMO_RELEASE_BUILD` esbuild define + env turbo), `release/1.0.0/` signé 4 plateformes + panel win-x64, `node tools/release/smoke.mjs` vert (`pnpm release:smoke` déclenche un bug pnpm 11 deps-check → lancer via node).
- **Environnement de test en place** : panel de test `D:\mmo-test\mmo-panel` (172.26.0.1:3100, admin / `Mmo-Test-2026!`), agent Windows service `mmo-agent` (LocalSystem, bundle corrigé 0.11.0 dans versions/), serveur `vanilla-1.20.1` desiredState running — **reboot Windows toujours à jouer** ; miroir d'échelle `D:\mmo-test\scale` (supprimable). WSL désinstallé.
- **Pièges résolus** : (1)–(51) inchangés (CLAUDE.md session 1 + docs). (52) `ServerRecord` porte `config.path`, pas `.path` (vitest ne typecheck pas — l'erreur a vécu jusqu'au test d'intégration). (53) `parallel`/simultané : toute allocation « lire l'état → sonder → écrire » de l'agent doit être sérialisée (RCON l'était pas). (54) ws : le message d'accueil serveur arrive dans le même tick que `open` côté client — poser les listeners avant/dans le constructeur. (55) Fastify+zod : la validation du body précède le handler — un limiteur de débit doit vivre en `preValidation`. (56) `pnpm release:smoke` échoue via le wrapper pnpm (deps-check) → `node tools/release/smoke.mjs`. (57) Mantine a11y : dimmed/placeholder/badges/nav basse sous 4,5:1 par défaut ; `Popover.Target` sur un `Indicator` pose aria-* sur un div (critical).
- **Dette / non fait** : critères manuels restants — **reboot Windows**, relais `sudo` interactif, Linux ARM réel, launchd macOS, migration Win→ARM, push Android/iOS réels, accès hors réseau ; dette sécurité résiduelle actée (doc 03 §6 « Dette restante ») ; `runtime.update` réel jamais exercé ; panel packagé pour l'hôte de build seulement ; `release/1.0.0` construit mais publié nulle part (pas de remote/CI) ; dette phases 9–10 inchangée.
- **Prochaine étape** : jouer les critères manuels (reboot Windows en premier — installation en place), publier la release dans le panel de test (`pnpm release:publish`) et exercer `agent.update`/`runtime.update` réels, puis décider : remote GitHub + CI + workflow release.yml, ou clôture.
