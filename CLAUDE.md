# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-22, fin de session phase 8)
- **Fait** : Phases 0 (spikes), 1 (monorepo), 2 (`protocol` + `shared`), 3 (`apps/agent`), 4 (`apps/panel` noyau), 5 (`apps/web` socle PWA), 6 (configuration et joueurs), 7 (monitoring et watchdog), **8** (tasks, backups, planificateur, transferts). `pnpm check` vert (20 tâches, **336 tests** : protocol 38, shared 106, agent 120 + 1 skip Linux, panel 35, web 37), `pnpm format:check` vert, **e2e Playwright 22/22** (`pnpm --filter @mmo/web e2e` : setup + `flow`/`whitelist`/`metrics`/`backups` × desktop/mobile × fr/en + `pwa`). **CI GitHub toujours pas observée** (aucun remote). Commits phase 8 : 5785deb (protocole + shared + agent), 46d96ab (panel), f63afde (web), docs (voir `git log`).
- **Phase 8 — protocole (par ajout, sans bump)** : `messages/tasks.ts` — `taskId` = ULID **fourni par l'initiateur** (panel pour les ordres, agent pour ses plannings ⇒ rejeu idempotent) ; requêtes P→A `task.cancel`/`task.ackResult`/`task.list`, `backup.create/list/restore/delete`, `fs.fetch` (URL → fichier jailé, sha256/sha1 optionnels) ; événements `task.progress` (non critique) et `task.completed`/`task.failed`/`backup.rotated` (**critiques**, journalisés + rejoués : c'est ce qui synchronise un backup exécuté panel éteint). `backupManifestSchema` (sha256 + taille = seule intégrité). `agent.configure.backupSchedules[]` `{ id, serverId, cron, keep?, keepDays?, onlyIfRunning, destination?, enabled }`, `backupDestination` ('' = défaut agent). `messages/transfer.ts` + `transfer/frame.ts` (frame `[1 o v][16 o transferId][8 o offset BE]`) + `transfer/engine.ts` (`TransferSender`/`TransferReceiver` partagés : chunks 1 Mo, fenêtre 8, priorité basse via `bufferedAmount`, `detach()/resume(offset)`, sha256 final) ; `fs.download.start` (`path` | `backupId`, `offset`), `fs.upload.start` (→ `offset` = taille du `.part`), `fs.transfer.ack/cancel` (événements) et `fs.transfer.done` (requête) en direction **`both`** (nouveau dans le catalogue : `RequestTypesTo/EventTypesTo` pour `handle`/`on`). `RpcTransport` : `sendBinary?/onBinary?/bufferedAmount?` ; `RpcPeer.sendBinary/onBinary`. Client : `taskDto`, `backupDto`, `backupPolicyDto/Input`, `scheduledTaskDto/Input` (`payload { command?, message?, warnMinutes?, timeoutSec? }`), `sparkStatus`, `panelBackupDto`, WS `task.update`/`backup.update`. Shared : `cron.ts` (5 champs, heure locale, règle OU dom/dow, `cronNext`), `node/codecs.ts` (`chunkCodec`, `createCompressStream` zstd 3 + checksum / gzip, `hasZstd`, jamais `nbWorkers` — **règle ESLint** partagée node/agent).
- **Phase 8 — agent 0.8.0** : `tasks/journal.ts` (`tasks.json`, write-ahead, `ctx.checkpoint()`), `tasks/runner.ts` (`TaskRunner` : `AbortSignal`, artefacts nettoyés, progression ≤ 2/s, au boot `recover()` ⇒ `E_INTERRUPTED` rejouable — appelé **après** création de la connexion pour être journalisé). `backup/tar.ts` (tar maison ustar + pax, `walkTree/tarEntries/extractTar`, chemins jailés), `backup/archive.ts` (`.tar.zst`/`.tar.gz` + `.part`, `verifyArchive`), `backup/backup-service.ts` (à chaud : `save-off`/`save-all flush`/`save-on` par RCON puis stdin + attente « Saved the game » ou silence 3 s ; destination requête > `store.backupDestination` > `<stateDir>/backups`, puis `/<serverId>/` ; exclusions `.mmo-trash logs crash-reports session.lock .mmo-server.json` ; rotation `keep/keepDays` par politique → `backup.rotated` ; restauration : vérif manifeste **avant** tout, stop, `pre_restore`, purge hors exclusions, extraction, relance), `backup/scheduler.ts` (tick 30 s, `backupScheduleRuns` anti-doublon, manquées non rattrapées), `transfer/transfers.ts` (download fichier/backup avec préfixe haché ; upload `.part` ; `fs.fetch`). Store : `backupDestination`, `backupSchedules`, `backupScheduleRuns`. Options `Agent` : `backupSchedulerTickMs` (0 = manuel), `saveSettleMs`, `fetchImpl`. `AGENT_CAPABILITIES = rcon, tasks, backups, transfers`. Fake server : `save-off/save-all/save-on` (journal `world/save-log.txt`). Faux panel de test : transport binaire.
- **Phase 8 — panel 0.8.0** : `services/tasks.ts` (ligne créée **avant** l'ordre, `progress` + phase/détail en mémoire → `task.update`, `stalled` à la déconnexion et au boot, `reconcile(task.list)`, purge 30 j), `services/backups.ts` (migration `0001` : `backups.manifest_json`, `task_id` ; `applyManifest`, `markDeleted`, `reconcile(backup.list)`, politiques → `schedulesFor` injecté dans `ServersService.buildAgentConfig`), `services/scheduler.ts` (actions exécutées par le panel : start/stop/restart/command/announce, avertissements `warnMinutes` = `say`, événement `schedule.run` + audit, `tickMs` 0 en test), `services/transfers.ts` (download → `PassThrough` avec contre-pression, reprise transparente après reconnexion (`reconnectWaitMs`) ; upload corps octet-stream), `services/panel-backup.ts` (`VACUUM INTO` quotidien via maintenance + `POST/GET /api/admin/backups`). Session : `task.*`, `backup.rotated`, `reconcilePhase8` après `pushConfig`, `transfers.bind`. Routes `http/routes/tasks.ts` : `/api/tasks`, `/api/servers/:id/backups[/…/restore|download]`, `backup-policies`, `schedules`, `files/download|upload`, `spark[/install]` (API `sparkapi.lucko.me/download`, `ctx.fetchImpl` injectable), `/api/admin/backups`. Tests `integration/phase8.test.ts`.
- **Phase 8 — web** : `api/phase8.ts` (clés `phase8Keys`, hooks, `uploadFile` XHR avec progression, URLs de download), `store/realtime.ts` (`applyTaskUpdate`, `applyBackupUpdate`, invalidations `task.completed/failed`, `backup.rotated`, `schedule.run`), `components/tasks/TaskProgress.tsx` (`TaskProgressRow`, `TasksIndicator` dans le header), `components/backups/BackupsPanel.tsx` (onglet **Sauvegardes** : table, créer, restaurer 1 clic (sécurité + relance cochées), télécharger (lien direct), supprimer, politiques), `components/schedule/CronInput.tsx` (préréglages, **composant contrôlé dérivé de `value`**) + `SchedulePanel.tsx` (onglet **Planificateur**), FileExplorer upload/download, MetricsPanel « installer spark en un clic ». Onglets `backups`/`schedule` dans `SERVER_TABS`. i18n `web:tasks.*`, `web:backups.*`, `web:schedule.*`, `common.edit`. Tests `BackupsPanel.test.tsx`, e2e `backups.spec.ts` (afterEach de nettoyage via l'API, `apiLogin` obligatoire sur le fixture `request`).
- **Pièges résolus (à ne pas réintroduire)** : (1)–(16) des phases 5–7 (voir docs 03/07 : `type="button"`, `POST …/stop` corps `{}`, `useT()`, `createLink`, `workbox-window`, alias PWA, clés i18n sans point, Switch/SegmentedControl Mantine = cliquer le `label`, `exactOptionalPropertyTypes` ⇒ `{...(cond ? { prop } : {})}`, e2e en cascade, pas de `python`, heredoc + `’` ⇒ scripts `.mjs` dans le scratchpad, fake server gelé, délai RCON couvre l'auth, vue watchdog à l'appel, flake de charge). (17) **Mantine `NumberInput` contrôlé** : après `user.clear` la frappe ne déclenche pas `onChange` en test ⇒ `tripleClick` + `keyboard` ; et ne jamais garder un état parallèle à `value` (closures périmées) — dériver. (18) **Clés i18n dynamiques** (`t` typé) ⇒ `tDynamic(i18n, key)`. (19) Les `task.failed` émis au boot de l'agent doivent passer par la connexion (journal) : `recover()` après `new AgentConnection`. (20) Un flux zstd tronqué est accepté sans erreur : toute intégrité = manifeste/sha256 (tests le prouvent). (21) `sed` sous Git Bash mange les `\\` dans les remplacements : pour des regex dans un fichier, utiliser Edit. (22) e2e : le fixture `request` de Playwright n'est pas authentifié ⇒ `apiLogin(request)` dans les hooks.
- **Dette / non fait (volontaire)** : migration agent→agent (phase 9) ; `java.install` (phase 11) ; reprise d'une task autrement que par offset de transfert (une task interrompue est simplement rejouable) ; upload de dossiers ; mode inspection 5 s non persisté ; MSPT en agrégés ; autocomplétion pseudos ; dates d'expiration de ban ; pages admin utilisateurs/réglages/sauvegardes du panel (API prête, `usePanelBackups` existe) ; Spotlight ; push (phase 10) ; bundle principal ~900 kB ; « plusieurs Go » de transfert non joué en CI (mécanique testée à chunks réduits).
- **Rappels phases 2–7** : protocole jalon A (`RpcPeer`, N/N-1) ; shared (i18n typée, Java manifest→table, parsing logs, détection 22/22, TPS) ; agent (connexion/backoff, lancement détaché, RCON maison, ré-adoption, `fs.*` jailé + corbeille, `config.*`, `player.action/resolve`, logs, échantillonneur CPU par cycles, `MetricsCollector`, `TpsProbe`, watchdog local) ; panel (Fastify 5 + Zod, Drizzle/`better-sqlite3`, sessions + RBAC, wizard, appairage/rotation, `/ws/agent` + `/ws/client`, adoption + conflits, `processed_events`, relais fichiers/config/joueurs, `MetricsService` + `metrics.db`, sert `apps/web/dist`) ; web (Vite 8 + React 19 + Mantine 8.3 + TanStack Router en code + Query + Zustand + xterm + i18next + PWA ; onglets joueurs/config/fichiers/journaux/métriques, graphiques SVG maison). Détails : notes « Implémentation » dans docs 03–07.
- **Environnement** : `node` global 22.14 (pnpm/vitest/Playwright tournent dessus ; **zstd absent ⇒ les tests exercent le repli gzip**, zstd réel seulement avec Node 24 ; `better-sqlite3` compilé pour cet ABI — `pnpm rebuild better-sqlite3` si on change de Node), Node 24.19 via `%LOCALAPPDATA%\pnpm\bin\node.EXE` ; Chromium Playwright installé ; PowerShell 5.1 présent. `apps/web/tsconfig.json` inclut `e2e` + `playwright.config.ts` (donc `apps/agent` est typé avec lib DOM par ce chemin : `response.body as unknown as AsyncIterable`). `.prettierignore`/`.gitignore` : `test-results`, `playwright-report`, `dev-dist`, `apps/panel/data`.
- **Prochaine étape** : **Phase 9 = multi-machines complet** (doc 07) : migration agent → agent (`migration.export`/`transfer.serve`/`migration.import`/`migration.finalize`, pré-checks cible : port libre, JRE, espace disque ; données directes HTTP one-shot token unique + reprise Range, fallback relais via les deux WebSockets — réutiliser `TransferSender/Receiver` et les archives de backup comme format d'export), `java.install` multi-fournisseur (Temurin → Zulu → x64 émulé, mode relais panel, task `java.install`), `agent.update` de bout en bout (bundle signé Ed25519, exit 75, launcher, health-check 30 s/2 crashs, **rollback réellement testé**) + `runtime.update`. Critère : migration réelle d'un serveur entre deux machines ; mise à jour d'agent poussée avec un bundle cassé → rollback constaté.
