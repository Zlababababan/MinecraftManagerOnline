# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-22, fin de session phase 5)
- **Fait** : Phases 0 (spikes), 1 (monorepo), 2 (`protocol` + `shared`), 3 (`apps/agent`), 4 (`apps/panel` noyau), **5** (`apps/web` socle PWA). `pnpm check` vert (20 tâches, 230 tests : protocol 30, shared 92, agent 66, panel 20, web 22), `pnpm format:check` vert, **e2e Playwright 10/10** (`pnpm --filter @mmo/web e2e`). **CI GitHub toujours pas observée** (aucun remote).
- **Phase 5 — front** (`apps/web`) : Vite 8 + React 19 + **Mantine 8.3.18** (+ `mantine-datatable` 8.3.13 ; **Mantine 9 existe depuis 2026-03-31**, décision verrouillée = 8, migration à décider plus tard) + TanStack Router (routes **en code**, `src/router.tsx`) + TanStack Query + Zustand + `@xterm/xterm` 6 (lazy, chunk séparé) + i18next/react-i18next + `vite-plugin-pwa`. Structure : `api/client.ts` (`ApiRequestError` code/status/details, `NetworkError`), `api/queries.ts` (clés + hooks + mutations, tout typé par `@mmo/protocol/client`), `ws/client.ts` (`RealtimeClient` : backoff, abonnements comptés, réabonnement, ping), `store/realtime.ts` (`applyServerMessage` → cache Query : `server.state`, `machine.heartbeat`, invalidations par type d'événement ; événements récents), `i18n/` (`web` ns fr/en + `@mmo/shared`, `useT()` typé sur 4 ns, `tDynamic` pour clés dynamiques, `i18next.d.ts`), `components/` (`Shell` AppShell + navigation basse mobile + menus thème/langue/compte, `links.tsx` = `createLink` sur enveloppes non polymorphes Mantine, `ServerCard`/`ServerActions`/`MachineHeader`/`PairingCodeCard`/`ConflictsPanel`/`EventsList`/`badges`, `console/ConsolePanel.tsx` xterm + miroir textuel caché `data-testid=console-mirror` + `console/commands.ts` complétion V1/historique), `pages/` (Setup, Login, Dashboard, Server [onglets overview/console/players/events/settings, `?tab=`], Machines [`?add=true`], Machine, Account, NotFound, Error), `app.tsx` (providers, `history` injectable), `pwa.tsx`. Icônes : `scripts/gen-icons.mjs` (`pnpm icons`).
- **Panel (phase 5)** : `http/static.ts` = `@fastify/static` + fallback SPA (`MMO_WEB_DIR`, défaut `apps/web/dist`, `DEFAULT_WEB_DIR` relatif à `config.ts`) ; auth « refus par défaut » **réduite à `/api/*` et `/ws/*`** (le reste est public) ; `stop`/`restart` acceptent un corps `null` (`nullish`) ; `PANEL_VERSION = 0.5.0`.
- **Pièges résolus (à ne pas réintroduire)** : (1) bouton « Suivant » et « Créer » du wizard = même nœud DOM réutilisé par React → le clic soumettait le formulaire ⇒ `key` distinctes + `type="button"` ; tout `<Button>` non-submit dans un `<form>` doit porter `type="button"`. (2) `POST …/stop` sans corps ⇒ Fastify passe `null` ⇒ le front envoie toujours `{}`. (3) i18next typé : les clés `ns:clé` ne sont acceptées que si `useTranslation` reçoit un **tableau** de ns ⇒ `useT()`. (4) `createLink` Mantine : composants polymorphes non typables ⇒ enveloppes `forwardRef` (`links.tsx`). (5) `workbox-window` doit être dépendance directe (pnpm strict). (6) Vitest : `virtual:pwa-register/react` aliasé vers `src/test/pwa-register.ts`.
- **Tests web** : `app.test.tsx` (routeur + gardes + login + dashboard + start contre `fetch` stubbé, `createMemoryHistory`), `ws/client.test.ts` (faux WebSocket), `store/realtime.test.ts`, `i18n.test.ts` (parité), `console/commands.test.ts`, `lib/format.test.ts`. E2E : `e2e/fixtures/stack.ts` (panel + agent réels in-process via `tsx`, routes `/e2e/*`), projets `setup` → `desktop-fr|en`, `mobile-fr|en` (Pixel 7), `pwa.spec.ts` (desktop-fr). La langue du **compte** prime sur celle du navigateur après login (le helper `login()` aligne le compte sur le projet).
- **Dette / non fait (volontaire)** : pages admin utilisateurs/réglages (API prête) ; Spotlight ; graphiques (phase 7) ; `metrics.sample` ignoré ; push non branché ; `fs.*`/`config.*`/`logs.*` (phase 6) ; tasks/backups (8) ; bundle principal ~890 kB (zod + Mantine + shared detection — à découper plus tard) ; Lighthouse non exécuté (plus de catégorie PWA depuis LH 12 ; critères vérifiés par `pwa.spec.ts`) ; onglets de la page serveur qui passent à la ligne sur mobile (acceptable).
- **Rappels phases 2–4** : protocole jalon A (33 req + 11 events, `RpcPeer`, négociation N/N-1) ; shared (i18n typée, Java manifest→table, parsing logs, détection 22/22) ; agent 0.3.0 (connexion/backoff, lancement détaché, RCON maison, ré-adoption) ; panel (Fastify 5 + Zod, Drizzle/`better-sqlite3`, sessions cookie + RBAC, wizard, appairage/rotation, `/ws/agent` + `/ws/client`, API machines/serveurs/console, adoption auto + conflits, `processed_events`). Détails : notes « Implémentation » dans docs 03–07.
- **Environnement** : `node` global 22.14 (pnpm/vitest/Playwright tournent dessus ; `better-sqlite3` compilé pour cet ABI — `pnpm rebuild better-sqlite3` si on change de Node), Node 24.19 via `%LOCALAPPDATA%\pnpm\bin\node.EXE` ; Chromium Playwright installé (`playwright install chromium`). `apps/web/tsconfig.json` inclut `e2e` + `playwright.config.ts` (types `node`). `.prettierignore`/`.gitignore` : `test-results`, `playwright-report`, `dev-dist`, `apps/panel/data`.
- **Prochaine étape** : **Phase 6 = configuration et joueurs** (doc 07) : agent `config.get/set` (properties avec préservation des clés inconnues, JSON whitelist/ops/bans, routage commandes-ou-fichiers selon l'état), API panel `config.*`/`fs.*`/`logs.*` relayées, éditeurs graphiques (server.properties expliqué, whitelist/ops/bans avec avatars + résolution UUID online/offline, EULA guidé), explorateur de fichiers (jail, corbeille `.mmo-trash/`) + éditeur texte, joueurs temps réel (kick/ban/pardon/op, historique `player_sessions`). Critère : e2e « gérer une whitelist depuis un téléphone sans jamais voir un fichier », serveur en marche vs arrêté.
