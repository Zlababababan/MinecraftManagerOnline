# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-22, fin de session phase 10)
- **Fait** : Phases 0 (spikes), 1 (monorepo), 2 (`protocol` + `shared`), 3 (`apps/agent`), 4 (`apps/panel` noyau), 5 (`apps/web` socle PWA), 6 (configuration et joueurs), 7 (monitoring et watchdog), 8 (tasks, backups, planificateur, transferts), 9 (migration agent → agent, Java géré, mises à jour d'agent), **10** (notifications push + centre in-app, couche d'accès tailscale/direct/manual, adresse à donner aux amis). `pnpm check` vert (20 tâches, **390 tests** : protocol 38, shared 110, agent 134 + 1 skip, panel 65, web 43 ; `sampler.test.ts` reste un flake de charge connu sous turbo, vert seul), `pnpm format:check` vert, **e2e Playwright 22/22** (inchangés). **CI GitHub toujours pas observée** (aucun remote). Commits phase 10 : 31e763d (protocole + shared + agent), 49dca04 (panel), 48cec23 (web), docs (voir `git log`).
- **Phase 10 — protocole client (par ajout, sans bump)** : `machineInfo.addresses { tailnet[], global[] }` (commun, porté par `pair.request`/`auth.hello`) ; `NOTIFICATION_TYPES` (12 catégories) + `NOTIFICATION_DEFAULTS` + `notificationTypeOf(event)` (partagé panel/web ; `server.stateChanged{state:'crashed'}` → `server.crashed`, `task.failed{kind:'backup.*'}` → `backup.failed`, `schedule.run` non-info → `schedule.failed`…) ; DTO `notificationPrefs`, `notificationsResult { notifications, unread, seenId }`, `pushSubscribe { endpoint, keys, userAgent? }`, `pushStatusDto { vapidPublicKey, subscriptions[] }` (endpoint tronqué), `pushPayload { title, body, url, tag, eventId, ts, locale }` ; accès : `accessModeSchema`, `dnsProviderSchema` (manual/duckdns/cloudflare/generic), `accessStatusDto` (mode, publicUrl, listen, https, tailscaleServeCommand, direct{domain, dnsProvider, dnsTokenSet, certificate, dyndns, pendingChallenge}, lastTest, requestVia), `accessTestResult` (http/ws/binary/tls/via/ok), `firewallRulesDto`, `serverAddressDto` (address, host, port, source machine|domain|detected|none, alternatives), `reachabilityResult` (Server List Ping) ; `updateMachineSchema.tailnetHost/publicHost`, `machineDto.addresses/tailnetHost/publicHost` ; `EDITABLE_SETTINGS` += `access.domain/httpsPort/dns.provider/dns.token/dns.zone/dns.updateUrl/acme.email/acme.directory/dyndns.enabled/publicHost` ; codes `E_PUSH_DISABLED` (409), `E_ACCESS_NOT_CONFIGURED` (409), `E_ACME_FAILED` (502), `E_DNS_FAILED` (502). Shared : `common:notify.<clé>.title/body` fr/en (clés camelCase **sans point** : serverCrashed, watchdogAlert, taskFailed, backupFailed, migrationDone/Failed, agentUpdateApplied/RolledBack, scheduleFailed, portConflict, serverRunning/Stopped, playerJoined/Left, test) + erreurs.
- **Phase 10 — agent 0.10.0** : `connection.ts` `networkAddresses(interfaces)` (pur, testé) embarqué dans `machineInfo()`. Rien d'autre côté agent.
- **Phase 10 — panel 0.10.0** : migration `0003_phase10` (`machines.addresses/tailnet_host/public_host`, `push_subscriptions.user_agent/last_seen_at`, `users.notifications_seen_id`). `services/push/webpush.ts` : **zéro dépendance** — `encryptPayload` (RFC 8291, aes128gcm un seul enregistrement, options salt/serverPrivateKey pour le vecteur de test), `decryptPayload` (tests), `vapidAuthorization` (JWT ES256 ieee-p1363, `aud` = origin), `sendWebPush(fetch, sub, payload, vapid, now, { topic, urgency, subject })` → `{ ok } | { gone (404/410), retryable }`. `services/notifications.ts` (`NotificationsService`, abonné au bus, file séquentielle, `flush()` en test) : `prefs/setPrefs`, `subscribe` (upsert par endpoint, change de compte), `unsubscribe`, `subscriptions`, `sendTest`, `list(userId, limit)` (SQL sur `BUS_TYPES` puis filtre `notificationTypeOf` + prefs, `unread` = id > seenId), `markSeen`, `deliver(event)` (utilisateurs actifs × prefs × abonnements, `render(event, locale)` via `createI18n` caché par langue, `notifyKey()`), purge sur `gone` ou 8 échecs, `subject` VAPID = `mailto:<access.acme.email>` > publicUrl https > `mailto:admin@localhost`. `services/access.ts` (`AccessService`) : `start(server)` branché par hook `onListen` dans `app.ts` (HTTPS si cert + timers DynDNS 10 min / renouvellement 24 h en mode direct), `status(headers)`, `tailscaleServeCommand()`, `certificateInfo()` (`X509Certificate`), `issueCertificate()` (une à la fois ; `pendingChallenge` exposé pour le mode manuel ; `<dataDir>/tls/{cert,key}.pem`, `account.json`, `state.json`), `ensureHttps()` (`https.createServer` → `upstream.emit('request'/'upgrade')`, `setSecureContext` si même hôte), `currentAddress()` (override > IPv6 vue au tick précédent > première), `updateDynDns()`, `firewallRules()`, `testReachability(url?)` (health + `probeTls` + `/ws/probe` 64 KiB via `ws`), `serverAddress(row)`, `testServer(row, address?)`. Helpers exportés : `requestVia(headers)`, `hostGlobalAddresses()`, `firewallCommands(os, port, label)`. `access/acme.ts` (`AcmeClient(account, { directoryUrl, fetchImpl, resolveTxt, pollIntervalMs, propagationTimeoutMs, statusTimeoutMs })`, `generateAccountKey`, `jwkThumbprint`, `dnsChallengeValue`, `LETS_ENCRYPT_DIRECTORY/_STAGING`), `access/der.ts` (`der.*`, `createCsr`, `sanExtension` — `ip:<addr>` accepté pour les tests —, `toPem`), `access/dns.ts` (`createDnsClient`, `DnsError`), `access/mcping.ts` (`pingMinecraft`, `parseAddress`, `formatAddress`). Options `buildApp({ access: { localAddresses, resolveTxt, acmeDirectory, acme, dyndnsIntervalMs, renewIntervalMs, renewBeforeDays } })`. Settings : `SETTING_KEYS.access*/dns*/acme*`, secrets = `push.vapidPrivateKey` + `access.dns.token` (`public()` renvoie `<clé>.set`). Routes `http/routes/phase10.ts` : `GET /api/push`, `POST /api/push/subscribe|unsubscribe|test`, `GET|PUT /api/notifications/prefs`, `GET /api/notifications`, `POST /api/notifications/seen`, `GET /api/access`, `GET /api/access/firewall` (admin), `POST /api/access/test|certificate|dyndns` (admin), `GET /api/servers/:id/address`, `POST /api/servers/:id/reachability` (operator) ; `/ws/probe` public dans `ws.ts`. `machines.update` accepte `tailnetHost/publicHost` ; `machineColumns` stocke `addresses`. Tests : `push/webpush.test.ts`, `notifications.test.ts`, `access/acme.test.ts`, `access/dns.test.ts`, `access.test.ts` ; helper `test/acme-fake.ts` (`startFakeAcme`, `createTestCa`, `issueTestCert`, `parseCsr`, `readTlv`).
- **Phase 10 — web** : `api/phase10.ts` (`phase10Keys`, hooks push/prefs/notifications/access/firewall/settings/address, mutations), `lib/push.ts` (`pushSupport()`, `isIos`, `isStandalone`, `subscribePush`, `unsubscribePush`, `currentSubscription`, `resyncPush`, drapeau `localStorage mmo.push`), `public/sw-push.js` (importé par Workbox via `workbox.importScripts` + `globIgnores`), `components/notifications/NotificationCenter.tsx` (cloche `notifications-open`, `notifications-indicator[data-unread]`, tiroir, `notifications-mark-seen`, lignes `notification-<id>[data-unread]`), `NotificationSettings.tsx` (`NotificationPrefsCard` : `pref-<type>` ; `PushCard({ support? })` : `push-enable/disable/test`, `push-ios-onboarding`, `push-unsupported-<reason>`, appareils), `components/access/PlayerAccessCard.tsx` (`expose-mode`, `player-address`, `player-address-copy/test/result[data-ok]`), `components/access/MachineHostsCard.tsx` (`machine-tailnet-host`, `machine-public-host`, `machine-hosts-save`), `components/admin/AccessCard.tsx` (`access-mode`, `tailscale-serve-command`, section directe `access-domain/https-port/dns-provider/dns-token/dyndns-enabled/direct-save`, `access-issue-certificate`, `access-pending-challenge`, `access-dyndns-now`, `access-published`, pare-feu `firewall-panel`, test `access-test-url/run/result[data-ok]/via`), `pages/SettingsPage.tsx` (route `/settings` admin : `settings-general` (`settings-public-url`, `settings-auto-update`, `settings-general-save`), `AccessCard`, `settings-push`), Shell : `PushResync` (au montage), `AccessIndicator[data-mode][data-ok]` (→ /settings si admin), `NotificationCenter`, entrée nav/menu `Réglages` (admin, `nav-settings`/`bottomnav-settings`/`menu-settings`), `store/realtime.ts` invalide `notifications` sur événement notifiable et `access` sur `access.*`. Compte : `PushCard` + `NotificationPrefsCard` ; vue d'ensemble serveur : `PlayerAccessCard` ; page machine : `MachineHostsCard`. i18n `web:notifications.*` (types en `server_crashed`… — point remplacé par `_`), `web:settings.*`, `web:access.*`, `web:playerAccess.*`, `nav.settings`. Test `notifications/notifications.test.tsx` (centre + temps réel, prefs PUT partiel, push iOS / supporté, accès joueurs).
- **Pièges résolus (à ne pas réintroduire)** : (1)–(29) des phases 5–9 (voir docs 03/07 et CLAUDE.md précédent). (30) **Les scripts `node -e "…"` en Bash mangent les backticks** (`\`` interprété par le shell) : tout patch contenant des template strings passe par un `.mjs` écrit avec l'outil Write. (31) Le typage strict i18next du web (`i18next.d.ts`) s'applique aussi au **panel** via l'inclusion `e2e` de `apps/web/tsconfig.json` : dans le panel, les clés dynamiques passent par un wrapper `tr(i18n, key, params)` (`notifications.ts`), pas par `i18n.t` direct. (32) `@types/ws` type `checkServerIdentity` en `boolean` alors que Node attend `undefined` : ne pas l'utiliser en test — la CA de test ajoute un SAN `IP:127.0.0.1` (`sanExtension('ip:…')`). (33) `createPublicKey(key).export({ type: 'spki' })` : un `KeyObject` privé ne s'exporte pas en SPKI directement. (34) `X509Certificate.checkIssued` compare le **nom** de l'émetteur : la CA de test porte son nom explicitement (`TestCa.name`), pas via une recherche dans le PEM. (35) Un champ privé `unsubscribe` et une méthode publique `unsubscribe()` ne peuvent coexister dans une classe (TS « private »).
- **Dette / non fait (volontaire)** : critère phase 10 « push reçu sur Android et iOS + panel accédé depuis un téléphone hors réseau dans les deux modes » **non rejoué physiquement** (mécanique testée en local : RFC 8291, faux ACME, proxy, HTTPS réel) — à valider à la main en phase 11 ; adresses IPv6 temporaires vs stables (heuristique + `access.publicHost`) ; renouvellement automatique impossible en DNS manuel ; pas d'e2e Playwright phase 10 (fixture sans SW push ni tailnet) ; pas d'onglet admin utilisateurs/sauvegardes du panel (API prête) ; dette phases 9 inchangée (migration physique Windows → Linux ARM, `runtime.update` réel, `java.remove` côté agent, reprise task, upload dossiers, mode inspection 5 s, MSPT, autocomplétion pseudos, bans à expiration, Spotlight, bundle web ~900 kB).
- **Rappels phases 2–9** : protocole jalon A (`RpcPeer`, N/N-1) + jalons B/C (tasks, backups, transferts binaires, `both`) + phase 9 (migration, java.install, agent.update signé + launcher rollback) ; shared (i18n typée, Java manifest→table + fournisseurs Temurin/Zulu, parsing logs, détection, TPS, cron, codecs) ; agent (connexion/backoff, lancement détaché, RCON, ré-adoption, `fs.*` jailé, config, joueurs, logs, métriques, watchdog, `TaskRunner`, backups, transferts, migration, Java installer, updater) ; panel (Fastify 5 + Zod, Drizzle, sessions + RBAC, wizard, appairage, `/ws/agent` + `/ws/client`, adoption, relais, métriques, tasks/backups/planificateur/transferts, `VACUUM INTO`, migrations, releases, Java géré, relais par jeton) ; web (Vite 8 + React 19 + Mantine 8.3 + TanStack + Zustand + xterm + i18next + PWA ; onglets serveur ; cartes migration/agent/Java/releases). Détails : notes « Implémentation » dans docs 03–07.
- **Environnement** : `node` global 22.14 (pnpm/vitest/Playwright ; **zstd absent ⇒ repli gzip en test**), Node 24.19 via `%LOCALAPPDATA%\pnpm\bin\node.EXE` ; Chromium Playwright installé ; PowerShell 5.1. `apps/web/tsconfig.json` inclut `e2e` + `playwright.config.ts` (⇒ `apps/panel` typé avec lib DOM et i18next typé par ce chemin). `.prettierignore`/`.gitignore` : `test-results`, `playwright-report`, `dev-dist`, `apps/panel/data`. ESLint web ignore `public/**` (SW push en JS brut).
- **Prochaine étape** : **Phase 11 = packaging et installation** (doc 07) : archives des 4 plateformes (runtime épinglé + bundles), `install.ps1`/`install.sh` servis par le panel, services (shawl / systemd / launchd), désinstallation, pipeline de release (build, signature avec clé hors dépôt — remplacer `tools/signing` dev —, publication dans le panel), documentation utilisateur (installation, ajout de machine, FAQ réseau : tailscale serve / direct IPv6 / DNS-01) ; **en profiter pour rejouer à la main les critères des phases 9 et 10** (migration Windows → Linux ARM, push Android/iOS, accès hors réseau en mode tailscale puis direct). Critère : installation from scratch sur un Windows vierge et un Linux ARM en suivant uniquement la doc, reboot compris.
