# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-22, fin de session phase 7)
- **Fait** : Phases 0 (spikes), 1 (monorepo), 2 (`protocol` + `shared`), 3 (`apps/agent`), 4 (`apps/panel` noyau), 5 (`apps/web` socle PWA), 6 (configuration et joueurs), **7** (monitoring et watchdog). `pnpm check` vert (20 tâches, **303 tests** : protocol 30, shared 99, agent 110 + 1 skip Linux, panel 31, web 32), `pnpm format:check` vert, **e2e Playwright 18/18** (`pnpm --filter @mmo/web e2e` : setup + `flow`/`whitelist`/`metrics` × desktop/mobile × fr/en + `pwa`). **CI GitHub toujours pas observée** (aucun remote). Commits phase 7 : 0dd8c52 (protocole + shared + agent), e01ed78 (panel), 6203f8f (web), docs (voir `git log`).
- **Phase 7 — protocole (par ajout, sans bump)** : `metrics.sample.servers[].tpsSource` (`neoforge|forge|spark|tick_query`, absent = indisponible), `watchdog.alert.kind = 'ram'` (action `none`), `tpsSourceSchema`/`TpsSource` dans `common.ts`. `@mmo/protocol/client` : `metricsQuerySchema` (`from`, `to?`, `resolution? raw|1m|1h`), `serverMetricsPointSchema`/`machineMetricsPointSchema` (brut = valeur ; agrégé = `cpu`/`ram`/`tps` moyens + `cpuMax`/`ramMax`/`tpsMin`/`samples`), `serverMetricsResultSchema`/`machineMetricsResultSchema` (`resolution, from, to, points, latest, tpsSource?, cpuSource`), message WS `{ type: 'metrics.sample', machineId, sample }`. Shared : `minecraft/tps.ts` (`parseForgeTps` 2 formats, `parseSparkTps`, `parseTickQuery`, `parseTpsResponse`, `tpsChain`), pattern `bind_failed` (`FAILED TO BIND TO PORT`).
- **Phase 7 — agent 0.7.0** (`src/monitoring/`) : `sampler.ts` — `PlatformSampler` choisit `WindowsCyclesSampler` (sidecar PowerShell embarqué `CYCLES_SCRIPT` via `-EncodedCommand`, cycles `QueryProcessCycleTime` + RSS + `% Processor Utility`, relancé s'il meurt, `mode: ticks` si `Add-Type` échoue) / `ProcSampler` (Linux `/proc`) / `PsSampler` (macOS `ps -o pid=,rss=,time=`) avec repli `TicksSampler` (`os.cpus()`, `cpuSource: 'ticks'`, pas de CPU par processus) ; **aucune dépendance** (dérogation doc 03 §1 : ni `pidusage` ni `systeminformation`). CPU % « en cœurs ». `metrics.ts` — `MetricsCollector` (`collect()` : cibles `manager.metricsTargets()` = serveurs `starting/running/stopping`, TPS seulement `running`, RAM/disque machine via `os`/`fs.statfs(stateDir)`, `summary` pour le heartbeat, tampon hors ligne `bufferMs/intervalMs` rejoué par `replay()` dans `onSession`, garde-fou RAM `> 1,5 × maxRamMb + 512` → `onRamExceeded` une fois par session). `tps.ts` — `TpsProbe` (chaîne `tpsChain`, spark = `mods/spark-*.jar`, méthode mémorisée, `retryAfterMs` 10 min quand tout échoue, `reset()` à chaque `starting`/`applyConfigs`). `watchdog.ts` — `Watchdog` (`onStateChanged`, `onRamExceeded`, `cancel`, `attempts`) : crash → `crashLoopMax` relances par fenêtre `crashWindowMs` 10 min, délai `min(5 s × n, 60 s)`, `crash_loop/gave_up` au-delà ; freeze = sonde RCON `list` toutes `clamp(freezeTimeoutSec/3, 5 s, 60 s)`, 3 échecs + processus vivant → alerte + `kill('freeze')` ; politique = `store.watchdog[serverId]` (persistée depuis `agent.configure.watchdog`). `ServerProcess` : `kill({ reason: 'freeze' })` → `crashed`/`freeze_kill`, événement `bind-failed` → `port.conflict`, `crashSignal` dans l'événement d'état. `RconClient.exec(cmd, timeoutMs)` applique désormais le délai **aussi à l'auth** (serveur gelé). Options `Agent` : `sampler`, `metricsIntervalMs` (0 = off ; tests/e2e), `watchdog` (délais). `agent.configure` : `watchdog` persisté + sondes relancées, `metricsIntervalSec` appliqué à chaud ; `server.start/stop/restart/kill` → `watchdog.cancel`. **Fake Java server** : `--freeze-after <ms>` (RCON muet y compris auth/junk, stdin ignoré), `--tps forge|neoforge|spark|tick|none` + `--tps-value`. Faux panel de test : `pause()/resume()` (panne simulée).
- **Phase 7 — panel 0.7.0** : `services/metrics.ts` `MetricsService` (`ingest` par lots 5 s/200 → `INSERT OR REPLACE` en transaction ; `maintain(now)` = brut→1 min→1 h sur tranches complètes avec reprise depuis `MAX(ts)` **ou** `dirtySince` (rejeux tardifs), purge 48 h/14 j/2 ans, `incremental_vacuum(200)` 1×/jour ; `queryServer/queryMachine` résolution auto brut ≤ 3 h / 1 min ≤ 3 j / 1 h ; `latest` en mémoire oublié par `forgetMachine` à la déconnexion ; `deleteServer`). Contexte : `metricsService`, `metricsSqlite` ; `runMaintenance` appelle `maintain` + checkpoint passif de `metrics.db`. Session : `metrics.sample` → `ingest` (serveurs inconnus filtrés) + diffusion WS ; `watchdog.alert` → événement + audit `watchdog.<action>` si action ≠ none. Routes `GET /api/servers/:id/metrics`, `GET /api/machines/:id/metrics`. Tests `services/metrics.test.ts` (50 h synthétiques, job horaire, agrégats exacts, idempotence, rejeu tardif, lots), `http/metrics.test.ts`.
- **Phase 7 — web** : `api/queries.ts` (`METRICS_RANGES` 1h/6h/24h/7d/30d, `keys.serverMetrics/machineMetrics(+All)`, `useServerMetrics`, `useMachineMetrics` ; `refetchInterval` 60 s sauf 1 h alimentée en direct), `store/realtime.ts` `applyMetricsSample` (met à jour `latest` partout, ajoute le point aux résultats `raw`, fenêtre glissante). `components/metrics/TimeSeriesChart.tsx` (SVG maison : `useElementSize`, trous = lignes coupées, bande min/max, référence, survol, `integer`, graduations au dixième), `MetricsPanel.tsx` (`ServerMetricsPanel` : « maintenant », plage, résolution annoncée, 4 graphiques, `tpsUnavailableReason` → raison honnête + lien spark « jamais requis », `CpuSourceWarning` ; `MachineMetricsPanel`). `ServerPage` : onglet `metrics` (2e), lazy. `MachinePage` : carte Métriques. `MachineHeader` : icône d'avertissement si `cpuSource = ticks`. `EventsList` : libellés `web:watchdog.kind/action.*` et port dans `port.conflict`. i18n `web:metrics.*`, `web:watchdog.*`. E2E `metrics.spec.ts` (fixture `metricsIntervalMs: 1000`).
- **Pièges résolus (à ne pas réintroduire)** : (1)–(11) des phases 5–6 (voir docs 03/07 : `type="button"`, `POST …/stop` corps `{}`, `useT()`, `createLink`, `workbox-window`, alias PWA, clés i18n sans point, Switch/SegmentedControl Mantine = cliquer le `label`, `exactOptionalPropertyTypes` ⇒ `{...(cond ? { prop } : {})}`, e2e en cascade, pas de `python`). (12) **Heredoc bash + apostrophe typographique `’`** ⇒ « unexpected EOF » : écrire les scripts d'édition dans le scratchpad (Write) puis `node fichier.mjs`. (13) Fake server gelé : ignorer **tout** le trafic RCON (auth et paquet junk compris), sinon l'`exec` résout avec une réponse vide et la sonde « réussit ». (14) `RconClient` : le délai d'`exec` doit couvrir l'auth, sinon chaque sonde sur serveur gelé coûte 5 s de plus. (15) La vue watchdog lit l'état **à l'appel** (`state: () => proc.state`), jamais un snapshot pris avant un `await`. (16) `pnpm check` a échoué une fois sur `@mmo/web test` sous charge (burner + jsdom en parallèle) puis passé en relance : flake de charge, pas de cause trouvée.
- **Dette / non fait (volontaire)** : « installer spark en un clic » (dépôt du jar par l'agent, phase 8 transferts) ; mode inspection 5 s non persisté (`metrics.intervalSec` global existe) ; MSPT absent des agrégats 1 min/1 h ; upload/download ; autocomplétion pseudos ; dates d'expiration de ban ; pages admin utilisateurs/réglages (API prête) ; Spotlight ; push (phase 10) ; bundle principal ~900 kB.
- **Rappels phases 2–6** : protocole jalon A (`RpcPeer`, N/N-1) ; shared (i18n typée, Java manifest→table, parsing logs, détection 22/22) ; agent (connexion/backoff, lancement détaché, RCON maison, ré-adoption, `fs.*` jailé + corbeille, `config.*`, `player.action/resolve`, logs) ; panel (Fastify 5 + Zod, Drizzle/`better-sqlite3`, sessions + RBAC, wizard, appairage/rotation, `/ws/agent` + `/ws/client`, adoption + conflits, `processed_events`, relais fichiers/config/joueurs, sert `apps/web/dist`) ; web (Vite 8 + React 19 + Mantine 8.3 + TanStack Router en code + Query + Zustand + xterm + i18next + PWA ; onglets joueurs/config/fichiers/journaux, catalogue `server.properties` 60 clés). Détails : notes « Implémentation » dans docs 03–07.
- **Environnement** : `node` global 22.14 (pnpm/vitest/Playwright tournent dessus ; `better-sqlite3` compilé pour cet ABI — `pnpm rebuild better-sqlite3` si on change de Node), Node 24.19 via `%LOCALAPPDATA%\pnpm\bin\node.EXE` ; Chromium Playwright installé ; PowerShell 5.1 présent (sidecar CPU testé : burner à ~100 % d'un cœur par cycles, ~2 % par ticks sous Hyper-V). `apps/web/tsconfig.json` inclut `e2e` + `playwright.config.ts`. `.prettierignore`/`.gitignore` : `test-results`, `playwright-report`, `dev-dist`, `apps/panel/data`.
- **Prochaine étape** : **Phase 8 = tasks, backups, planificateur** (doc 07) : mécanisme **tasks** (jalon B : table `tasks`, progression, annulation, journal WAL agent, reprise, réconciliation au boot du panel), **transferts binaires** (jalon C : chunks, fenêtre, reprise par offset, priorité basse — branchés sur download/upload de l'explorateur ; en profiter pour « installer spark en un clic »), backups (création à chaud `save-off/save-all`, restauration avec backup de sécurité, rotation locale agent + `backup.rotated`, destination configurable, `VACUUM INTO` du panel), planificateur (cron UI, start/stop/restart programmés + annonces côté panel, plannings de backups poussés à l'agent). Critère : backup planifié exécuté **panel éteint** puis synchronisé à la reconnexion ; restauration 1 clic vérifiée par checksum ; download de plusieurs Go avec coupure/reprise.
