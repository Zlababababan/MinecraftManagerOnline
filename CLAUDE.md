# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-22, fin de session phase 9)
- **Fait** : Phases 0 (spikes), 1 (monorepo), 2 (`protocol` + `shared`), 3 (`apps/agent`), 4 (`apps/panel` noyau), 5 (`apps/web` socle PWA), 6 (configuration et joueurs), 7 (monitoring et watchdog), 8 (tasks, backups, planificateur, transferts), **9** (migration agent → agent, Java géré, mises à jour d'agent). `pnpm check` vert (20 tâches, **356 tests** : protocol 38, shared 110, agent 132 + 1 skip Linux, panel 38, web 38), `pnpm format:check` vert, **e2e Playwright 22/22** (inchangés : setup + `flow`/`whitelist`/`metrics`/`backups` × desktop/mobile × fr/en + `pwa`). **CI GitHub toujours pas observée** (aucun remote). Commits phase 9 : 8b13b1d (protocole + shared + agent + `tools/signing`), b412ae1 (panel), 9c5ac80 (web), docs (voir `git log`).
- **Phase 9 — protocole (par ajout, sans bump)** : `messages/migration.ts` — `migration.export { taskId, serverId, migrationId, backupId, codec?, destination?, announce?, stopTimeoutSec? }` (task → manifeste + `wasRunning`), `transfer.serve { serverId, backupId, token 32 hex (panel), ttlSec } → { urls[], size, sha256, expiresAt }`, `migration.precheck { serverId, path, gamePort?, javaMajor?, javaStrict?, requiredBytes } → { ok, path, port, java { runtime?, installable? }, disk { freeBytes? } }` (codes `path_not_empty/parent_missing/port_in_use/java_missing/disk_full`), `migration.import { taskId, migrationId, config, manifest, sources[] { url, kind: direct|relay, headers? }, connectTimeoutMs?, startAfter }` (task → `{ serverId, path, files, bytes, source, started }`), `migration.finalize { serverId, migrationId, path, action: rename|keep } → { path, renamed, purgeAfter? }`. `messages/java.ts` — `java.install { taskId, majorVersion, sources[] { vendor, url (absolue ou relative au panel), archive zip|tar.gz, sha256?, size?, emulated, relay, headers?, fullVersion? } }`, `java.remove { path } → { removed }`. `messages/update.ts` — `agent.update { version, url, sha256, signature (base64), size?, headers?, runtimeVersion? } → { accepted, currentVersion, alreadyCurrent }`, `runtime.update { version, os, arch, url, sha256, archive } → { accepted, currentVersion, pending }`, événement **critique** `agent.updateResult { eventId, ts, kind agent|runtime, status applied|rolled_back, version, otherVersion?, reason? }`. `auth.hello.runtimeVersion` (ajout). Codes : `E_PRECHECK_FAILED` (`details.checks`), `E_SIGNATURE_INVALID`, `E_UNREACHABLE` ; panel `E_NO_RELEASE`. `TASK_KINDS` += `migration.export/import`, `java.install`. `KNOWN_CAPABILITIES` += tasks/backups/transfers/migration/java/update. Client : `migrationDto` (+ `migrationStatus`), `startMigrationSchema { toMachineId, toDirectoryId?, toPath?, restartAfter, installJava, announce? }`, `migrationPrecheckDto` (+ `toPath`), `javaRuntimeDto`, `installJavaSchema { majorVersion, relay, vendor? }`, `agentReleaseDto`, `publishReleaseQuerySchema`, `updateAgentSchema`, `machineDto.runtimeVersion/latestRelease/updateAvailable`, réglage `agents.autoUpdate`, WS `migration.update`. Shared : `java/providers.ts` (`javaCandidates`, URLs/parse Adoptium + Azul, `compareVersions`), `MIGRATED_DIR` (`/\.migrated-\d{8}-\d{4}$/`) exclu du scan.
- **Phase 9 — agent 0.9.0** : `util/download.ts` (`downloadWithResume` : sources dans l'ordre, `Range` sur le `.part`, délai de connexion 5 s direct / 30 s sinon, 3 tentatives, sha256/taille, URL relative résolue contre `panelHttpOrigin(panelUrl)`), `migration/migration.ts` (`AgentMigration` : export = stop + `backups.create(pre_migration)` ; `serve` = un `http.Server` par adresse privée (`privateAddresses()`, option `serveAddresses` en test), fermé après transfert complet ou TTL ; `precheck` (`checkPath`, `isPortFree`, `java.select`, `statfs`) ; `importServer` : staging `<stateDir>/migrations/<id>/`, `verifyArchive`, `extractArchive`, `manager.upsertConfig` (nouveau), relance ; `finalize` : `applyConfigs(others)`, rename avec 10 tentatives EPERM/EBUSY sinon laissé en place, marqueur retiré, `store.migratedDirs` purgés à 7 j avec la corbeille), `java/installer.ts` (`JavaInstaller` : dossier `<stateDir>/java/<major>-<vendor>[-x64]/`, `.downloads/*.part`, `<home>.extract` aplati, sonde injectable `javaProbe` — aussi passée à `JavaRegistry` —, `remove` géré seulement), `java/zip.ts` (extraction zip maison, deflate en flux, pas de ZIP64), `update/updater.ts` (`AgentUpdater` : `home` = option `agentHome` ou `MMO_AGENT_HOME`, clés `update/keys.ts` (option `updatePublicKeys`), `update → versions/<v>/agent.js + next.json` puis `restart(75)` (option `exit`), `updateRuntime → runtime/<v>/ + runtime-next.json`, `consumeUpdateResult()` émis à la 1re session, `notifyHealthy()` IPC), `launcher/launcher.cjs` (copié dans `dist/` ; `MMO_LAUNCHER_HEALTH_MS` en test). `extractTar` : `stripComponents`, `preserveMode`, `symlinks` (internes à `dest`). Store : `migratedDirs`, rename de `agent-state.json` avec reprise EPERM. `sync.state.javaRuntimes` rempli (sondes au boot). Options `Agent` : `agentHome`, `updatePublicKeys`, `agentVersion`, `serveAddresses`, `javaProbe`. `ensureServerIdle` couvre `migration.export`. Tests : `agent.migration.test.ts` (2 agents + faux panel : direct, relais, annulation, dossier non vide), `update/launcher.test.ts`, `update/updater.test.ts`, `java/installer.test.ts` ; `buildZip` dans `test/helpers.ts`.
- **Phase 9 — panel 0.9.0** : migration `0002_phase9` (`machines.runtime_version`, `server_migrations.source_path/to_path/mode/export_task_id/import_task_id/restart_after`). `services/relay.ts` (`RelayTokens` : jetons 32 hex multi-usage jusqu'à TTL, payload bundle|java|migration ; `ctx.relayTokens` — `ctx.relay` reste le `ConsoleRelay`), `services/releases.ts` (`publish` flux → `<dataDir>/releases/agent-<v>.js`, `latest`, `updateAvailable`, `pushUpdate` → `agent.update` par jeton 30 min, `maybeAutoUpdate` après `reconcilePhase8`, `applyUpdateResult` → événements `agent.updateApplied/RolledBack` + audit), `services/java-runtimes.ts` (`sync` depuis `sync.state`/`java.list`, `planSources` via `ctx.fetchImpl` (API fournisseurs ; 404 = sauté), relais = cache `<dataDir>/jre-cache/` + jeton 1 h, `install` = ligne `tasks` avant l'ordre, `remove`), `services/migrations.ts` (`MigrationsService` : `resolveTarget` (`toPath` ou `<dir>/<basename source>`), `precheck`, `start` (pré-checks bloquants sauf Java installable + `installJava`, `provisioning = migrating`), `run` : export → Java à la volée → `transfer.serve` + jeton relais → import (sources directes puis `/api/relay/<token>`) → `servers.moveToMachine` (nouveau, `setProvisioning`) → `pushConfig` ×2 → `finalize` ; progression via `tasks.subscribe` (nouveau, avec `waitForFinish`) ; au boot, actives ⇒ `failed E_INTERRUPTED`). Session : `runtimeVersion`, `javaRuntimes.sync`, `agent.updateResult`, manifeste `migration.export` → `backups`. Routes `http/routes/phase9.ts` : `GET/POST /api/servers/:id/migrations[/precheck]`, `GET /api/migrations/:id`, `GET /api/machines/:id/java`, `POST …/java/install`, `DELETE …/java/:runtimeId`, `GET /api/agent-releases`, `PUT/DELETE /api/admin/agent-releases[/:version]`, `POST /api/machines/:id/update`, `POST …/runtime-update`, **`GET|HEAD /api/relay/:token` public** (`Range` 206/416 ; migration = `transfers.download(offset)`). `machineDto` via `machineUpdateFields`. `app.ts` transmet désormais `schedulerTickMs`/`transferReconnectWaitMs`/`migrationTtlMs` au contexte (avant : ignorés). Test `integration/phase9.test.ts` (fetch du panel redirigé vers un faux fournisseur local).
- **Phase 9 — web** : `api/phase9.ts` (`phase9Keys`, hooks, `api.putBinary`), `store/realtime.ts` (`applyMigrationUpdate`, invalidations `migration.done/failed`, `agent.update*`), `components/migration/MigrationPanel.tsx` (`MigrationsCard` dans la vue d'ensemble : modale cible/répertoire ou chemin/relance/installJava/annonce, **pré-checks** listés `data-testid=check-<label>`, bouton `migration-start` activé seulement si tout est ok (Java installable + case cochée accepté), suivi `migration-active`, historique), `components/machine/AgentCard.tsx` (version/runtime/latest, `agent-update`, 3 derniers événements), `components/machine/JavaCard.tsx` (table, `java-major`/`java-relay`/`java-install`, suppression), `components/admin/ReleasesCard.tsx` (MachinesPage, admin : publication fichier + signature). i18n `web:migration.*`, `web:java.*`, `web:agentUpdate.*`, `web:releases.*`, kinds `migration_export/import`, `java_install`, phases `probing/registering/starting/done`, `common.date/status`. Test `MigrationPanel.test.tsx`.
- **Pièges résolus (à ne pas réintroduire)** : (1)–(22) des phases 5–8 (voir docs 03/07 et CLAUDE.md précédent : `type="button"`, `POST …/stop` corps `{}`, `useT()`, `createLink`, `workbox-window`, alias PWA, clés i18n sans point, Switch/SegmentedControl Mantine = cliquer le `label`, `exactOptionalPropertyTypes` ⇒ `{...(cond ? { prop } : {})}`, e2e en cascade, pas de `python`, scripts `.mjs` dans le scratchpad, fake server gelé, délai RCON couvre l'auth, vue watchdog à l'appel, flake de charge, `NumberInput` = `tripleClick` + `keyboard`, `tDynamic`, `recover()` après `new AgentConnection`, zstd tronqué accepté, `sed` mange les `\\`, `apiLogin(request)`). (23) **Les heredocs Git Bash mangent aussi les `\\`** (même quotés) : tout script d'édition contenant des antislashs ou des regex s'écrit avec l'outil Write, jamais via `cat <<'EOF'`. (24) `ServerManager.get(id)` ne renvoie un `ServerProcess` que s'il a été créé (démarré) : pour un serveur configuré mais jamais lancé, lire `store.getServer(id)`. (25) Sous Windows, renommer le dossier d'un serveur qui vient de s'arrêter ⇒ EPERM transitoire (réessayer) ; idem `agent-state.json` sous charge. (26) `probeJavaVersion` : `execFile` peut lever **synchronement** (`spawn UNKNOWN`) sur un fichier non exécutable ⇒ `try/catch`. (27) `app.ts` doit transmettre explicitement chaque option de contexte (`schedulerTickMs`… étaient silencieusement ignorés). (28) Les assertions sur les `task.progress` intermédiaires sont flaky (≤ 2/s, petit fichier) : asserter le résultat, pas la phase. (29) Une option `Agent` capturée à la construction (`serveAddresses`) ne se remplace pas après coup : injecter une closure sur une variable de test.
- **Dette / non fait (volontaire)** : migration **entre deux machines physiques** (Windows → Linux ARM) non jouée — agent non packagé avant la phase 11 ; e2e Playwright de migration (fixture à un seul agent) ; `server_migrations.status = rolled_back` inutilisé ; mini-protocole d'amorçage « figé à vie » non formalisé ; clé de signature de release (dev commitée → hors dépôt en phase 11) ; `runtime.update` non joué avec un vrai runtime Node (mécanique testée avec un faux `node.exe`) ; `java.remove` ne vérifie pas les serveurs qui utilisent le JRE côté agent (UI : désactivé si `usedBy > 0`) ; reprise d'une task autrement que par offset ; upload de dossiers ; mode inspection 5 s ; MSPT ; autocomplétion pseudos ; bans à expiration ; pages admin utilisateurs/réglages/sauvegardes du panel (API prête) ; Spotlight ; push (phase 10) ; bundle web ~900 kB.
- **Rappels phases 2–8** : protocole jalon A (`RpcPeer`, N/N-1) + jalons B/C (tasks, backups, transferts binaires `TransferSender/Receiver`, `both`) ; shared (i18n typée, Java manifest→table, parsing logs, détection 22/22, TPS, cron, codecs zstd/gzip) ; agent (connexion/backoff, lancement détaché, RCON maison, ré-adoption, `fs.*` jailé + corbeille, `config.*`, joueurs, logs, CPU par cycles, métriques, watchdog, `TaskRunner` + journal WAL, backups à chaud/restauration/rotation/plannings, transferts) ; panel (Fastify 5 + Zod, Drizzle/`better-sqlite3`, sessions + RBAC, wizard, appairage/rotation, `/ws/agent` + `/ws/client`, adoption + conflits, `processed_events`, relais fichiers/config/joueurs, métriques, tasks/backups/planificateur/transferts, `VACUUM INTO`) ; web (Vite 8 + React 19 + Mantine 8.3 + TanStack Router/Query + Zustand + xterm + i18next + PWA ; onglets joueurs/config/fichiers/journaux/métriques/sauvegardes/planificateur). Détails : notes « Implémentation » dans docs 03–07.
- **Environnement** : `node` global 22.14 (pnpm/vitest/Playwright tournent dessus ; **zstd absent ⇒ les tests exercent le repli gzip**, zstd réel seulement avec Node 24 ; `better-sqlite3` compilé pour cet ABI — `pnpm rebuild better-sqlite3` si on change de Node), Node 24.19 via `%LOCALAPPDATA%\pnpm\bin\node.EXE` ; Chromium Playwright installé ; PowerShell 5.1 présent. `apps/web/tsconfig.json` inclut `e2e` + `playwright.config.ts` (donc `apps/agent` est typé avec lib DOM par ce chemin : `response.body as unknown as AsyncIterable`). `.prettierignore`/`.gitignore` : `test-results`, `playwright-report`, `dev-dist`, `apps/panel/data`. Plusieurs JDK réels sur la machine : l'inventaire Java d'un agent de test les sonde (`java -version`) sauf `javaProbe` injectée.
- **Prochaine étape** : **Phase 10 = notifications et couche d'accès** (doc 07) : Web Push (VAPID — clés `push.vapid*` déjà réservées dans `app_settings` —, `push_subscriptions`/`notification_prefs` (tables existantes), préférences par type d'événement, localisation par destinataire, onboarding iOS guidé, re-synchronisation des abonnements morts, centre de notifications in-app), couche d'accès pluggable **sans API Tailscale** (mode `tailscale` : `tailscale serve` documenté + testé WS et frames binaires ; mode `direct` : ACME DNS-01 intégré + client DynDNS + règles pare-feu affichées ; mode `manual`), `expose_mode` par serveur + test de joignabilité + « adresse à donner aux amis ». Critère : push reçu sur Android et iOS (PWA installée) pour un crash simulé ; panel accédé depuis un téléphone hors du réseau local dans les deux modes.
