# CLAUDE.md — MinecraftManagerOnline (MMO)

Fichier de contexte pour Claude uniquement. Style volontairement dense. **À maintenir** : mettre à jour la section « État » à chaque fin de phase, décision structurante, ou avant de proposer un changement de chat.

## Projet
Pilotage à distance de serveurs Minecraft auto-hébergés. Architecture : panel central (web + API + SQLite) + agents (1 par machine, WebSocket sortant, appairage par code). Multi-OS Win/Linux/macOS, x64 + ARM64. PWA responsive FR/EN, thème sombre. Licence propriétaire. Pas de MVP : application complète (périmètre = doc 02).

## Source de vérité = docs/ — lire À LA DEMANDE, ne pas dupliquer ici
- 01-presentation : vision, principes, glossaire
- 02-fonctionnalites : périmètre V1/Futur (11 domaines)
- 03-socle-technique : stack, distribution/updates, couche d'accès, sécurité, tests, spikes
- 04-base-de-donnees : schéma SQLite complet + règles d'exploitation
- 05-protocole : catalogue des messages panel↔agent, tasks, transferts, pannes
- 06-minecraft : lancement (4 templates), détection, console, cycle de vie, RCON, TPS, fichiers
- 07-plan-de-developpement : 13 phases (0–12), dépendances, critères « terminé quand »

## Décisions verrouillées (résumé télégraphique)
TS partout · Node 24 LTS épinglé · Fastify 5 + Zod 4 · React 19 + Vite + Mantine 8 + TanStack + xterm · SQLite WAL, 2 fichiers (mmo.db + metrics.db) · Drizzle · pnpm + Turborepo · agent = bundle esbuild universel + runtime Node embarqué + micro-launcher (exit 75, health-check 30 s/2 crashs, rollback N-1, signature Ed25519, clé privée hors panel) · AUCUN module natif dans l'agent · RCON auto-provisionné (stdin = principal, RCON = complément/mode detached) · serveurs Java détachés, survivent à l'agent, ré-adoption PID+heure+cmdline · couche d'accès pluggable tailscale(défaut)/direct(IPv6+ACME DNS-01+DynDNS)/manual, ZÉRO API Tailscale dans le code · Java multi-fournisseur Temurin→Zulu→x64 émulé, mapping via manifest Mojang · métriques 15 s · timestamps epoch ms partout · jamais `.strict()` sur les schémas protocole · protocole évolue par ajout, panel supporte N/N-1 · erreurs = codes (l'UI traduit) · i18n fr/en dès la 1re chaîne · panel = autorité des IDs serveurs · compression gzip (zstd si spike OK)

## Environnement utilisateur
Windows 11 · PC en IPv6 seul (CGNAT IPv4, pas de redirection de ports) · serveurs réels dans `E:\Minecraft\Server` (~56 : Forge/NeoForge/Fabric/Vanilla, 1.12→1.21, structures hétérogènes) = terrain de test → **travailler sur des copies, ne rien modifier/casser** · git : identité locale neutre déjà configurée sur le repo (MinecraftManagerOnline / noreply) — ne JAMAIS utiliser le nom/mail perso de l'utilisateur, ni dans les commits ni ailleurs.

## Méthode de travail
- Français, concis. L'utilisateur valide chaque phase avant la suivante ; les décisions déjà validées ne se rediscutent pas.
- Une phase = code + tests + docs amendés (toute dérogation aux docs 03–06 est actée dans le doc concerné) + commit(s) en français.
- Économie de contexte : lire uniquement les docs/fichiers utiles à la tâche en cours ; ne pas re-scanner `E:\Minecraft\Server` ; ne pas relire des docs déjà appliqués dans la session.
- **Règle de rotation de chat** : dès que le contexte devient lourd (fin de phase, long débogage clos, pivot de sujet), (1) mettre à jour la section « État » ci-dessous, (2) proposer spontanément à l'utilisateur un prompt prêt-à-coller pour démarrer un nouveau chat qui reprend exactement où on en est.

## État (maj 2026-08-22, fin de session phase 6)
- **Fait** : Phases 0 (spikes), 1 (monorepo), 2 (`protocol` + `shared`), 3 (`apps/agent`), 4 (`apps/panel` noyau), 5 (`apps/web` socle PWA), **6** (configuration et joueurs). `pnpm check` vert (20 tâches, **259 tests** : protocol 30, shared 92, agent 85, panel 25, web 27), `pnpm format:check` vert, **e2e Playwright 14/14** (`pnpm --filter @mmo/web e2e` : setup + `flow`/`whitelist` × desktop/mobile × fr/en + `pwa`). **CI GitHub toujours pas observée** (aucun remote). Commits phase 6 : 0b8bcc2 (protocole + agent), 55d1150 (panel), web + tests + docs (voir `git log`).
- **Phase 6 — protocole** : `player.action` (`kick|ban|pardon|banIp|pardonIp|op|deop|whitelistAdd|whitelistRemove`, `target`, `reason?`, `level?` → `{ applied: file|commands, response?, warnings? }`) et `player.resolve` (`names[]` → `{ players: [{ name, uuid|null, source: usercache|mojang|offline|unknown }], onlineMode }`) ajoutés au catalogue (35 req + 11 events, fixtures de contrat à jour). `messages/fs.ts` : `CONFIG_DATA_SCHEMAS` / `CONFIG_SET_SCHEMAS` (properties = patch `{ clé: valeur|null }`, JSON = tableau complet, champs de ban optionnels), `config.set` → `{ applied, restartRequired, commands?, warnings?, sha256? }`, `CONFIG_WARNINGS` (`W_OP_LEVEL_LIVE`, `W_BAN_EXPIRES_LIVE`, `W_COMMAND_FAILED`, `W_KICK_REQUIRES_RUNNING`). `@mmo/protocol/client` : DTO `playerSessionDto`, `playerActionRequest`, `playerResolveRequest`, `configSetRequest`, `fsPathQuery` (`path` défaut `''`), `fsMoveBody`, `fsWriteBody`, `logsSearchRequest`.
- **Phase 6 — agent 0.6.0** : `src/files/jail.ts` (`normalizeRelative`, `Jail.resolveChecked` = `realpath` du parent existant ⊂ racine), `files/fs-service.ts` (`FsService` : list/stat/mkdir/rename/copy/delete→`.mmo-trash/<epoch>-<nom>` + sidecar `.mmo-trash.json`/read ≤ 512 Ko/write atomique `writeAtomic` + `expectedSha256`, `purgeTrash` 7 j appelé par l'agent au démarrage + toutes les 6 h), `files/logs.ts` (`listLogFiles`, `searchLogs` en flux, gunzip, limit 500/5000), `minecraft/config-files.ts` (`ConfigService` : `get`/`set`/`playerAction`, routage `isRunning()` = starting/running/stopping → commandes via `exec` (manager : RCON 5 s puis stdin), sinon fichiers ; properties toujours fichier + `whitelist on|off` à chaud), `minecraft/players.ts` (`offlineUuid` v3 MD5, `readUserCache`, `resolveMojang` bulk 10, `resolvePlayers`, `FetchLike` injectable). `ServerManager` : `files(id)`, `config(id)`, `resolvePlayers`, `listLogFiles`, `searchLogs`, `purgeTrash`, options `fetchImpl`/`now`. Handlers `fs.*`, `config.*`, `logs.*`, `player.action/resolve` dans `agent.ts`. **Fake Java server** : `whitelist add|remove|list|on|off|reload`, `op/deop`, `ban/pardon`, `ban-ip/pardon-ip`, `kick` → écrit les JSON dans le cwd comme le vrai (UUID hors ligne).
- **Phase 6 — panel 0.6.0** : `http/routes/files.ts` (config/fs/logs relayés, écritures `operator` + audit + événements `server.configChanged`/`server.fileChanged`), `routes/servers.ts` (`POST …/players/resolve`, `POST …/players/action` → audit `player.<action>` + événement `player.action`, `GET …/players/history` via `ServersService.playerHistory`). Tests `http/files.test.ts` (faux agent `RpcPeer` qui enregistre les requêtes).
- **Phase 6 — web** : `api/queries.ts` (clés `config/configAll/playerHistory/files/filesAll/fileRead/logFiles`, hooks `useConfigFile<F>`, `useSetConfig`, `useResolvePlayers`, `usePlayerAction`, `usePlayerHistory`, `useFiles`, `useFileRead`, `useFileMutations`, `useLogFiles`, `useLogSearch` ; `api.put`), `store/realtime.ts` (`SERVER_SCOPED_INVALIDATIONS` par type d'événement), `lib/properties-catalog.ts` (60 clés : type bool/int/enum/string, catégorie, défaut, `live` pour `white-list`, `managed` pour RCON ; `diffProperties`, `validateValue`, `groupProperties` ; i18n `web:properties.keys.<clé avec . → _>` dans `i18n/locales/properties-{en,fr}.ts`), composants `players/` (`PlayerAvatar` mc-heads + repli initiales, `PlayersPanel` : vues online/whitelist/ops/bans/history, `AddPlayerForm` = resolve puis action), `config/PropertiesEditor.tsx` (accordéon par catégorie, clés inconnues en texte, patch minimal, `expectedSha256`, bandeau restart + bouton), `config/EulaCard.tsx` (modale guidée), `files/FileExplorer.tsx` (breadcrumbs, menu par entrée, `TextEditor` modal avec `E_CONFLICT` → rechargement), `logs/LogsPanel.tsx`. `ServerPage` : 8 onglets (`overview|console|players|config|files|logs|events|settings`, `Tabs.List` dans un `ScrollArea` horizontal), panneaux lourds en `lazy`.
- **Pièges résolus (à ne pas réintroduire)** : (1)–(6) de la phase 5 (bouton non-submit ⇒ `type="button"` + `key` ; `POST …/stop` corps `{}` ; `useT()` ; `createLink` enveloppes ; `workbox-window` dépendance directe ; alias `virtual:pwa-register/react`). (7) **i18next** : une clé contenant un point est un chemin ⇒ clés de propriétés `rcon.port` → `rcon_port` (`propertyI18nKey`), vérifié par `properties-catalog.test.ts`. (8) Switch Mantine : `<input>` caché 0×0 ⇒ en e2e cliquer `label[for=id]` (`toggleWhitelist` dans `whitelist.spec.ts`) ; un Switch **contrôlé** ne change d'état qu'après l'aller-retour ⇒ jamais `setChecked`. (9) `exactOptionalPropertyTypes` : ne pas passer `prop={cond ? x : undefined}` à Mantine ⇒ `{...(cond ? { prop: x } : {})}`. (10) Un test e2e qui échoue laisse le serveur en marche ⇒ les projets suivants cascadent ; relancer seul avant de conclure. (11) `python` indisponible dans le shell ; utiliser Node pour les scripts.
- **Tests** : agent `files/fs-service.test.ts`, `files/logs.test.ts`, `minecraft/players.test.ts` (Notch hors ligne = `b50ad385-829d-3141-a216-7e7d7539ba7f`), `minecraft/config-files.test.ts` (arrêté = fichiers ; en marche = commandes contre le fake server), `agent.test.ts` étendu ; panel `http/files.test.ts` ; web `lib/properties-catalog.test.ts`, `components/players/PlayersPanel.test.tsx` ; e2e `e2e/whitelist.spec.ts` (fixture `stack.ts` en `online-mode=false`, fichiers vérifiés sur disque via `/e2e/info`).
- **Dette / non fait (volontaire)** : upload/download de fichiers et archives de logs (phase 8, transferts) ; autocomplétion des pseudos depuis `usercache` ; édition des dates d'expiration des bans ; pages admin utilisateurs/réglages (API prête) ; Spotlight ; graphiques + `metrics.sample` (phase 7) ; push ; bundle principal ~900 kB ; `CONFIG_DATA_SCHEMAS` typés mais `config.get.data` reste `unknown` dans le protocole (le front le caste par fichier via `ConfigGetResult<F>`).
- **Rappels phases 2–5** : protocole jalon A (`RpcPeer`, négociation N/N-1) ; shared (i18n typée, Java manifest→table, parsing logs, détection 22/22) ; agent (connexion/backoff, lancement détaché, RCON maison, ré-adoption) ; panel (Fastify 5 + Zod, Drizzle/`better-sqlite3`, sessions cookie + RBAC, wizard, appairage/rotation, `/ws/agent` + `/ws/client`, adoption auto + conflits, `processed_events`, sert `apps/web/dist`) ; web (Vite 8 + React 19 + Mantine 8.3 + TanStack Router en code + Query + Zustand + xterm + i18next + PWA). Détails : notes « Implémentation » dans docs 03–07.
- **Environnement** : `node` global 22.14 (pnpm/vitest/Playwright tournent dessus ; `better-sqlite3` compilé pour cet ABI — `pnpm rebuild better-sqlite3` si on change de Node), Node 24.19 via `%LOCALAPPDATA%\pnpm\bin\node.EXE` ; Chromium Playwright installé. `apps/web/tsconfig.json` inclut `e2e` + `playwright.config.ts`. `.prettierignore`/`.gitignore` : `test-results`, `playwright-report`, `dev-dist`, `apps/panel/data`.
- **Prochaine étape** : **Phase 7 = monitoring et watchdog** (doc 07) : agent `metrics.sample` 15 s (CPU/RSS par serveur + machine, `cpuSource`), panel → `metrics.db` (lots + transactions groupées, downsampling 1 m/1 h + purge), graphiques historiques (uPlot si besoin), TPS (chaîne de fallback + « installer spark en un clic » + « TPS indisponible » honnête), watchdog agent (crash faisceau doc 06 §4, freeze par sondes RCON, auto-restart borné `crash_loop_max`, garde-fou RAM, conflits de ports). Critère : crash et freeze simulés sur fake server → événements corrects, auto-restart borné, graphiques exacts après 48 h de données synthétiques.
