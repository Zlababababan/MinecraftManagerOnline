# 03 — Socle technique

Décisions validées le 2026-08-21. Ce document est la référence : toute dérogation en cours de développement doit être documentée ici.

## Décisions clés en un coup d'œil

| Sujet | Décision |
|---|---|
| Langage | **TypeScript partout** (panel, agent, front) |
| Runtime | Node.js 24 LTS, épinglé, identique panel/agent |
| Base de données | SQLite (WAL) — `mmo.db` + `metrics.db` séparés |
| Distribution agent | Bundle JS universel (~3 Mo, tous OS/arch) + runtime Node embarqué + micro-launcher |
| Accès distant | Couche d'accès **pluggable** : `tailscale` (défaut) / `direct` (IPv6 + domaine) / `manual` |
| RCON | **Auto-provisionné par défaut** (dépendance architecturale, pas une option) |
| Réseau dans le code | **Aucune API Tailscale** — le réseau est une abstraction |

## 1. Stack

| Couche | Choix | Notes |
|---|---|---|
| Runtime | **Node.js 24 LTS** (plancher : 22) | Même version épinglée panel + agent |
| Langage | TypeScript 5.x, `strict: true`, ESM | |
| API panel | **Fastify 5** + `fastify-type-provider-zod` | Zod = validation runtime + types partagés |
| Temps réel | `ws` via `@fastify/websocket` | 2 endpoints : `/ws/agent` (protocole) et `/ws/client` (front) |
| Auth | Sessions cookie maison + **argon2** (`@node-rs/argon2`), RBAC en hook Fastify | Pas de JWT (même origine, peu d'utilisateurs) |
| Push | ~~`web-push`~~ → **implémentation maison** (`apps/panel/src/services/push/webpush.ts`) | *Phase 10* : RFC 8291 (aes128gcm) + RFC 8292 (VAPID ES256) avec `node:crypto` seul, vérifiée contre le vecteur de test de la RFC 8291 (annexe A) ; `fetch` injectable (tests = faux endpoint). Zéro dépendance, comme le reste du panel hors socle. |
| Front | **Vite + React 19**, TanStack Router + Query, Zustand (état temps réel) | |
| UI | **Mantine 8** (+ `mantine-datatable`) | AppShell responsive, thème sombre natif, Spotlight |
| Console | `@xterm/xterm` + addons fit/search | |
| Graphiques | Mantine Charts (Recharts) ; **uPlot** en secours pour le temps réel haute fréquence | |
| i18n | i18next + react-i18next, fichiers `fr`/`en` dans `packages/shared` | Le panel localise aussi les push |
| PWA | vite-plugin-pwa (Workbox) | |
| ORM | **Drizzle ORM + drizzle-kit** (migrations SQL commitées), driver **`node:sqlite`** (adaptateur maison, §3) | `better-sqlite3` retiré le 2026-08-30 |
| Monorepo | pnpm workspaces + Turborepo | |
| Bundling agent | esbuild → un seul `agent.js` CJS | **Règle : aucun module natif dans l'agent** |
| Tests | Vitest, Playwright, « fake Java server » | Voir §9 |

> **Implémentation (phase 4, `apps/panel`)** : Fastify 5 + `fastify-type-provider-zod` (schémas Zod = validation + types des routes, erreurs de validation → `E_VALIDATION`), `@fastify/websocket` (`/ws/agent`, `/ws/client`), `@fastify/cookie`, Drizzle sur **`node:sqlite`** (adaptateur `src/db/sqlite.ts`, voir l'amendement ci-dessous), `@node-rs/argon2` (argon2id, OWASP m = 19 MiB / t = 2 / p = 1). Le **contrat panel ↔ front** (DTO REST, messages de `/ws/client`, codes d'erreur HTTP) vit dans **`@mmo/protocol/client`**, distinct du protocole panel↔agent mais sous les mêmes règles (jamais `.strict()`). Toute erreur HTTP sort en `{ code, message, retryable, details }`, traduite par l'UI. Config par env : `MMO_DATA_DIR` (défaut `./data`), `MMO_HOST` (défaut `127.0.0.1`, `0.0.0.0`/`::` **refusés au démarrage**), `MMO_PORT`, `MMO_COOKIE_SECURE`, `MMO_MOJANG_MANIFEST`, `MMO_LOG_LEVEL`.
>
> **Amendement (2026-08-30) — sortie de `better-sqlite3`, driver `node:sqlite`.** Motif : un module natif est un `.node` lié à la glibc de la machine qui l'a construit. C'est ce qui a rendu le panel ininstallable sur une VM Oracle Ubuntu 20.04 ARM64 (aucun prebuild pour Node 24/arm64 → node-gyp → `build-essential`), puis ce qui a cassé les archives Linux des versions 1.0.2 et 1.0.3. `node:sqlite` est embarqué dans le runtime : **plus aucun module natif SQLite, donc plus de plancher de glibc issu du code**.
> - **Adaptateur** `apps/panel/src/db/sqlite.ts` : présente la surface de better-sqlite3 réellement consommée (`prepare/exec/pragma/transaction/close`, `run/all/get/raw`). Trois manques de `node:sqlite` y sont comblés — il n'a **ni `pragma()` ni `transaction()`**, ses erreurs n'ont **pas de code `SQLITE_*`** (seulement un `errcode` numérique, traduit ici : sans quoi les gardes `error.code === 'SQLITE_CONSTRAINT_UNIQUE'` de `servers.ts` — course d'adoption — et `=== 'SQLITE_CANTOPEN'` de `main.ts` — message « chown » — deviennent des `if` toujours faux, **sans la moindre erreur visible**), et ses lignes ont un **prototype nul**. Les transactions imbriquées basculent en `SAVEPOINT` comme le faisait better-sqlite3.
> - **Drizzle 0.45.2 n'a pas de driver `node:sqlite`**, et `drizzle-orm/better-sqlite3` importe le module natif dès sa première ligne. La base est donc assemblée dans `db/client.ts` à partir de `BetterSQLiteSession` + `BaseSQLiteDatabase` (sous-chemins `/session` et `/migrator`, vérifiés sans aucun `.node` dans `process.moduleLoadList`) — c'est la recopie du `construct()` du driver. `@types/better-sqlite3` reste en devDependency : le `.d.ts` de la session en dépend (types seulement).
> - **`drizzle-orm/sqlite-proxy` a été écarté** en connaissance de cause : il existe et il est officiel, mais il est **asynchrone** — l'adopter transformerait les 32 services et les tests synchrones du panel en `Promise`.
> - Garde-fou : règle ESLint `no-restricted-imports` (`kind: 'panel'`) sur `better-sqlite3`, `drizzle-orm/better-sqlite3` et `**/*.node`. `GET /api/health` expose `sqlite: { driver, version }`.
>
> **Amendement (2026-08-30) — `mmo-panel setup`, configuration sans navigateur.** Le wizard n'existait qu'en HTTP alors que le panel n'écoute que sur 127.0.0.1 : sur une VM cloud il fallait monter un tunnel SSH ou un `tailscale serve` **avant** de pouvoir créer le premier compte (troisième accroc de l'installation Ubuntu 20.04 ARM). `completeSetup` sort de la closure de route vers `services/setup.ts` — impératif, c'est lui qui génère les clés VAPID, et une seconde implémentation créerait une installation dont le push est mort sans que rien ne le dise. La CLI (`src/cli-setup.ts`) ouvre le contexte complet **sans serveur HTTP** : `--username` plus **une seule** source de mot de passe parmi `--password-stdin` / `--password-file` / `--random-password` (jamais en argument : la ligne de commande est visible de toute la machine), `--locale`, `--public-url` (normalisée par `coerceOrigin` comme dans le wizard), `--access-mode` ; refus si un compte existe déjà.
>
> **Amendement (2026-08-30) — point d'entrée et `mmo-panel doctor`.** `main.ts` n'importe plus rien de l'application au niveau module : il vérifie la version de Node puis charge `boot.ts` (démarrage) ou `doctor.ts` par `await import()`. Motif : un runtime inadapté fait échouer la **construction du graphe de modules**, donc avant la première ligne exécutable et avant l'ouverture du journal — l'utilisateur n'avait qu'une stack de chargeur. `mmo-panel doctor` rend des contrôles `{ code, level, message }` : runtime (version, plateforme, glibc), `node:sqlite` et `@node-rs/argon2` chargés par `createRequire` (argon2 est réellement exercé par un hachage), dossier de données (écriture **réelle** par `openSync('w')` — un `stat` ment sous ACL — et propriétaire comparé à `process.getuid()` avec la commande `chown` exacte), `mmo.db` (`quick_check`), port, front. Sortie 1 dès qu'un contrôle est en erreur. Volontairement borné : ni détection de pare-feu, ni dérive d'horloge, ni reconnaissance de l'hébergeur.

> **Implémentation (phase 5, `apps/web`)** : Vite 8 + React 19 + **Mantine 8.3** (+ `mantine-datatable` 8 ; Mantine 9 est sorti le 2026-03-31 — migration à décider plus tard, hors périmètre), TanStack Router (routes **en code**, pas de génération de fichiers) + TanStack Query (cache = source unique des données REST ; les messages `/ws/client` y sont projetés), Zustand (statut temps réel, événements récents), `@xterm/xterm` 6 + `addon-fit` (chargé à la demande, chunk séparé), i18next/react-i18next, `vite-plugin-pwa` (Workbox `generateSW`, `registerType: autoUpdate`, `/api` et `/ws` exclus du fallback de navigation). Le front consomme **`@mmo/protocol/client` tel quel** (DTO, messages WS validés par `serverMessageSchema`, codes d'erreur). Client `/ws/client` : reconnexion avec backoff (1 s → 15 s), abonnements comptés par canal, réabonnement automatique à la reconnexion (le panel renvoie un snapshot ; l'UI déduplique par `seq`), ping 30 s ; codes 4001/1008 = pas de reconnexion. Le **panel sert le build** (`@fastify/static`, `MMO_WEB_DIR`, défaut `apps/web/dist`) avec fallback SPA sur `index.html` pour toute navigation hors `/api` et `/ws` ; `index.html`, `sw.js` et le manifest sont servis `no-cache`, `/assets/*` immuables. En dev, Vite proxifie `/api` et `/ws` vers le panel (`MMO_PANEL_URL`, défaut `http://127.0.0.1:3000`). Icônes PWA générées sans dépendance (`scripts/gen-icons.mjs`, encodeur PNG minimal).

### Pourquoi TypeScript partout (et pas un agent Go)

- La logique « intelligente » de l'agent (détection heuristique, parsing de logs, mapping MC→Java, watchdog) doit exister aussi côté panel : en TS elle vit une fois dans `packages/shared`, en Go elle serait dupliquée ou générée.
- Le gain RAM de Go (~30 Mo vs ~50–140 Mo) est sans valeur sur des machines dimensionnées pour des heaps Java de 4–16 Go (Raspberry Pi 4/5 inclus).
- Le modèle de distribution (§3) neutralise l'avantage single-binary de Go : l'artefact de mise à jour est un unique bundle JS identique pour toutes les plateformes.
- **Condition de bascule assumée** : si la cible devient des machines à 512 Mo–1 Go, réécrire l'agent en Go redevient pertinent. Le protocole versionné (Zod → JSON Schema exportable) borne ce coût : seule l'implémentation de l'agent changerait.

### Bibliothèques sensibles de l'agent

- Process : `child_process.spawn` **détaché** (`detached: true` — indispensable sous Windows, sinon libuv place l'enfant dans un Job Object tué avec l'agent) + stdin/stdout/stderr pipés + persistance PID (voir doc 05, ré-adoption). **Validé par le spike n°1** ([`docs/spikes/01-eof-stdin.md`](spikes/01-eof-stdin.md)) : tous les loaders testés survivent à la mort de l'agent et à l'EOF stdin, restent pilotables par RCON et s'arrêtent proprement.
- Métriques — **amendé le 2026-08-21 par le spike n°2** ([`docs/spikes/02-monitoring-windows.md`](spikes/02-monitoring-windows.md)) : sous Windows, toute comptabilité CPU « par ticks » (`GetProcessTimes`, WMI, compteurs `% Processor Time`, donc `pidusage` et `systeminformation`) est fausse d'un facteur 25–60× dès qu'Hyper-V est actif (WSL2, Docker, VBS). L'agent utilise donc :
  - **Windows** : un **sidecar PowerShell persistant** (script embarqué dans le bundle, P/Invoke `QueryProcessCycleTime` via `Add-Type`, compteur `% Processor Utility` pour la machine) — aucun module natif ; repli `pidusage` + drapeau `cpuSource: 'ticks'` si PowerShell est indisponible.
  - **Linux / macOS** : `pidusage` (`/proc`, `ps`) — comptabilité exacte.
  - `systeminformation` pour l'inventaire (OS, CPU, RAM, volumes) et `mem()`/`fsSize()` périodiques (session `powerShellStart()` sur Windows) ; jamais pour la charge CPU Windows.
  - Les deux bibliothèques fonctionnent **sans `wmic`** (Windows 24H2+), vérifié.

> **Implémentation (phase 7, `apps/agent/src/monitoring/sampler.ts`) — dérogation actée** : **ni `pidusage` ni `systeminformation` ne sont embarqués** (zéro dépendance supplémentaire dans le bundle). Le sidecar PowerShell (script embarqué en chaîne, lancé via `-EncodedCommand`, stdin/stdout JSON ligne à ligne) mesure les **cycles** par processus (`QueryProcessCycleTime`) + RSS (`WorkingSet64`) + `% Processor Utility` ; si `Add-Type` échoue le script bascule lui-même sur `TotalProcessorTime` et le signale (`mode: ticks`) ; si PowerShell ne démarre pas, repli `os.cpus()` machine seule (`cpuSource: 'ticks'`, aucun CPU par processus). Linux : `/proc/<pid>/stat` (utime+stime, CLK_TCK 100) + `/proc/<pid>/status` (VmRSS) + `/proc/stat` (`cpuSource: 'proc'`). macOS : `ps -o pid=,rss=,time=` (temps CPU cumulé → delta) + `os.cpus()` (`'proc'`). RAM machine = `os.totalmem/freemem`, disque = `fs.statfs` du volume de l'état de l'agent. CPU % « en cœurs » (100 = un cœur nominal saturé, borné à `cores × 100`). Le test « burner » du spike n°2 est un test d'intégration de l'agent (`sampler.test.ts`, Windows seulement : > 80 % d'un cœur par cycles).
- RCON : implémentation maison (~100 lignes, protocole trivial) avec file de commandes sérialisée.
- Backups : ~~`archiver` (zip/tar streaming) dans un **worker_thread**~~ — **amendé en phase 8** : tar maison (`apps/agent/src/backup/tar.ts`, ustar + pax, générateur asynchrone + `pipeline()`) dans le processus de l'agent, sans dépendance ni worker (la lecture disque domine ; zstd est natif dans `node:zlib`). Compression par défaut — **amendé le 2026-08-21 par le spike n°3** ([`docs/spikes/03-zstd-node24.md`](spikes/03-zstd-node24.md)) : **zstd niveau 3** (`node:zlib`, Node ≥ 22.15, 5–9× plus rapide que gzip à ratio égal), `checksumFlag` activé, **gzip en repli** (runtime sans zstd ou choix utilisateur). Règles absolues : jamais `ZSTD_c_nbWorkers` (perte silencieuse de données constatée) ; l'intégrité d'une archive repose sur le **manifeste (sha256 + taille)**, jamais sur le codec (un flux zstd tronqué est accepté sans erreur par Node).

## 2. Structure du monorepo

```
mmo/
├─ apps/
│  ├─ panel/        # Fastify, ws, drizzle, web-push ; sert le front buildé + les artefacts agent
│  ├─ web/          # React PWA
│  └─ agent/        # bundle esbuild + launcher.js
├─ packages/
│  ├─ protocol/     # schémas Zod du protocole + client/serveur RPC typés
│  ├─ shared/       # i18n fr/en, mapping MC→Java, parsing de logs, heuristiques de détection
│  └─ config/       # tsconfig/eslint partagés
├─ docs/
└─ turbo.json, pnpm-workspace.yaml
```

## 3. Distribution, installation, mises à jour

### Modèle de distribution (panel et agent, même mécanique)

Archive par plateforme (win-x64, linux-x64, linux-arm64, darwin-arm64) = `runtime/` (Node officiel épinglé) + `app/` (bundle JS) + `launcher.js` + outil de service. Les archives agent sont **embarquées dans la release du panel et servies par lui** :

- Windows : `irm https://<panel>/install.ps1 | iex`
- Linux/macOS : `curl -fsSL https://<panel>/install.sh | sh`

### Installation du panel en une commande (2026-09-01, lot 2)

`apps/panel/install/install-panel.sh` (POSIX sh, Linux/systemd, servi en asset de release GitHub — le panel n'a pas de panel amont) : télécharge `panel-<plat>.json` puis l'archive depuis `releases/latest/download/` (ou `--version`, ou `--archive` hors ligne), vérifie sha256, refuse musl. **Séparation code/données/config** : code `/opt/mmo-panel` (remplacé à chaque mise à jour, bascule `.new`→`.old`), données `/var/lib/mmo-panel` (jamais touchées, `StateDirectory`), réglages `/etc/mmo-panel/panel.env` (jamais écrasé ; `MMO_DATA_DIR` n'y vit PAS — cycle interdit, et l'unit borne les écritures via `ReadWritePaths`). Unit générée depuis le **gabarit versionné embarqué dans l'archive** (`app/install/mmo-panel.service.tmpl` — une seule source, réécrite à chaque update, personnalisation par drop-in `systemctl edit`) : durcissement complet **sauf** `MemoryDenyWriteExecute` (tue V8), `AmbientCapabilities=CAP_NET_BIND_SERVICE` (port 443 du mode direct), `ExecStartPre=-… doctor`, `ExecStart` via `mmo-panel.sh` (pas de chemin de runtime codé en dur dans l'unit). Relancer le script = **mise à jour** : sauvegarde de `mmo.db` d'abord, swap, `systemctl restart` explicite (`enable --now` ne redémarre pas un service actif), attente de `/api/health` **avec comparaison de version**, rollback vers `.old` sinon (version en échec conservée en `.failed`). Rétrocompat : des données dans `<dir>/data` sont détectées, préservées à travers les swaps et jamais déplacées ; `--migrate-data` opt-in (copie, `integrity_check` via node:sqlite embarqué, suppression de l'ancien dossier seulement après redémarrage sain). Jamais de lecture stdin (`curl | sh` : stdin est le script). Exercé en CI sur `debian:11` nu (pose hors ligne, doctor, mise à jour par-dessus des données à l'ancien emplacement).

**Docker** : image multi-stage `node:24-bookworm-slim` (la réponse à Alpine/musl — l'image emporte sa libc ; aucun runtime épinglé), étape finale non-root (`USER node`), healthcheck sans curl (fetch du runtime), `VOLUME /data` possédé par `node` (compose recommande le volume nommé — un bind `./data` créé par root = SQLITE_CANTOPEN reproduit). `MMO_ALLOW_ANY_INTERFACE=1` : opt-in **explicite et bruyant** (warn au boot) qui lève le refus de `0.0.0.0` — jamais déduit d'une détection de conteneur (échoue en Podman rootless) ; posé par l'image, la ligne `ports:` du compose décide de l'exposition réelle. Image officielle publiée par `release.yml` sur ghcr.io (buildx amd64+arm64, arm64 sous QEMU) avec les archives d'agents de la release déposées dans `docker/dist-agent/` (entrypoint : `MMO_DIST_DIR=/app/dist-agent` si présent et non surchargé — sans agents, défaut `<dataDir>/dist` sur le volume, le dépôt admin y persiste). CI : l'image est construite ET démarrée sur chaque PR (`/api/health`, warn vérifié, `doctor`).

**Bannière « version X disponible »** : `services/update-check.ts` lit `releases.atom` (pas d'API GitHub, pas de quota) au plus 1 fois/6 h — timer dédié de 30 min dans `buildApp` (PAS `runMaintenance` : les tests l'appellent directement, et un appel au démarrage ferait sortir chaque panel de test sur GitHub ; aucun panel de test ne vit 30 min), auto-limitation par `panel.update.checkedAt` horodaté AVANT l'appel (GitHub en panne n'est pas martelé). Le flux Atom liste tout (pré-releases comprises, titres libres, ordre chronologique) : on parse les tags `/releases/tag/vX.Y.Z` stricts (pré-releases exclues) et on prend le max par `compareVersions`. Événement `panel.updateAvailable` publié une fois par version (`panel.update.notifiedVersion`), catégorie de notification `panel.update` (groupe `panel`, cloche oui / push non par défaut). Exposition front par `/api/auth/me` (`panelUpdate`, admins seulement), bannière dans le Shell (fermeture par version en localStorage). Réglage `panel.updateCheck.enabled` (défaut activé) : un panel auto-hébergé ne doit pas être forcé de parler à GitHub.

**Windows** : `install-panel.ps1` (même contrat — asset de release, sha256, `.new`/`.old`, health+version+rollback, `-Uninstall [-Purge]`) : code `%ProgramFiles%\mmo-panel`, données `%ProgramData%\mmo-panel`, service **shawl** recréé à chaque passage (le chemin `runtime\<v>\node.exe` est re-résolu — un bump de `.node-version` ne casse plus le service), démarrage `delayed-auto`, `LocalSystem` par défaut (`-ServiceAccount User` + SeServiceLogonRight via secedit pour les lecteurs réseau), choix (`-Port`, compte…) mémorisés dans `%ProgramData%\mmo-panel\install.json` et relus à la mise à jour, `-MigrateFrom` copie les données d'une installation manuelle (integrity_check, original jamais modifié — la suppression reste à l'utilisateur). Auto-élévation UAC calquée sur `install.ps1` de l'agent. **Icône de zone de notification** : `mmo-panel-tray.ps1` (WinForms NotifyIcon, zéro dépendance), raccourci menu Démarrer posé par l'installeur ; l'icône PILOTE le service quand il existe (Start/Stop/Restart élevés à la demande, jamais un second panel) et sinon lance le panel en enfant (« Quitter » l'arrête — arrêt abrupt assumé, WAL) ; « Démarrer avec Windows » = raccourci shell:Startup par utilisateur. Pièges PowerShell notés dans les scripts : BOM UTF-8 obligatoire (5.1 lit le sans-BOM en ANSI, un « — » devient une smart quote qui FERME les chaînes), `-LiteralPath` (crochets = glob), pas de guillemets imbriqués dans un `node -e` (passage d'arguments natif < 7.3).

### Démarrage au boot

- **Windows** : service via **shawl** (embarqué dans l'archive ; fallback documenté : WinSW). **Identité : compte de l'utilisateur** (pas LocalSystem — ACL sur les dossiers serveurs, lecteurs mappés). À tester sur un poste réel.
- **Linux** : unit systemd (`User=mmo`, `Restart=on-failure`).
- **macOS** : LaunchDaemon (`KeepAlive=true`).
- **Contrainte commune (spike n°1)** : le superviseur ne doit **jamais tuer l'arbre de processus** de l'agent, sinon les serveurs Java tombent avec lui. Un `taskkill /T` sur l'agent tue les serveurs détachés (constaté) ; idem `KillMode=control-group` (défaut systemd) ou un groupe de processus launchd. À imposer en phase 11 : systemd `KillMode=process` (ou serveurs lancés dans un scope transient), launchd `AbandonProcessGroup=true`, Windows : vérifier que shawl/WinSW n'enrôlent pas l'enfant dans un Job Object « kill on close » — test dédié par OS.
- **Vérifié sur reboot Windows réel (2026-08-24, service shawl/LocalSystem)** : service redémarré seul, serveur `desiredState=running` **relancé** par `restoreOnBoot` ~40 s après le boot, événements mis en file (`pendingEvents`) tant que le panel était injoignable puis rejoués à la reconnexion ; arrêts/redémarrages du service ultérieurs : serveur Java survivant et **ré-adopté** (PID+heure+cmdline), console via RCON. Piège d'environnement : ne jamais lier panel/agent à une IP de commutateur virtuel (Hyper-V/WSL — le sous-réseau change ou disparaît au reboot) ; préférer une adresse stable (loopback en local, tailnet sinon).

### Mise à jour de l'agent (modèle unifié — fait autorité)

1. Chaque release du panel embarque le bundle agent de même version + sha256 + **signature Ed25519**. La clé privée de signature vit **chez le mainteneur** (jamais sur le panel) ; la clé publique est embarquée dans l'agent — un panel compromis ne peut pas pousser du code arbitraire.
2. Au `auth.hello`, l'agent annonce sa version ; le panel propose `agent.update` (politique auto ou validation admin).
3. L'agent télécharge **depuis le panel** le bundle universel, vérifie sha256 + signature, écrit `versions/x.y.z/`, note `next.json`, sort avec le code **75**.
4. Le **launcher** (~150 lignes, figé, sur-testé, jamais mis à jour par ce canal) bascule `current` (junction/rename — pas de symlink sous Windows), relance. **Health-check : WS + heartbeat rétablis sous 30 s et pas 2 crashs**, sinon rollback automatique vers N-1 + signalement.
5. **Les serveurs Minecraft ne tombent jamais** : processus Java détachés, ré-adoptés au redémarrage de l'agent (vérification PID **+ heure de démarrage du process + ligne de commande** — jamais le PID seul). Stdin perdu jusqu'au prochain restart du serveur → pilotage RCON entre-temps (mode `detached`).
6. Le **runtime Node** a son propre canal (`runtime.update`, rare) : téléchargement de l'archive runtime, swap par le launcher au prochain restart.

> **Implémentation (phase 9)** : `apps/agent/launcher/launcher.cjs` (copié dans `dist/` par le build) + `apps/agent/src/update/updater.ts` ; disposition `home/` = `versions/<v>/agent.js`, `current.json`, `next.json`, `trial.json`, `update-result.json`, `runtime/<v>/`, `runtime-next.json`/`runtime-current.json`, `launcher.log`. Santé = message IPC `healthy` (session panel établie) ; rollback N-1 après 30 s sans santé ou 2 crashs ; **testé** avec un bundle volontairement cassé (`launcher.test.ts` : crash → crash → rollback + `update-result.json`), un bundle muet (`health_timeout`) et une bascule réussie (`applied`). Signature : Ed25519 via `node:crypto` (`verify(null, …)`), clés publiques embarquées dans `src/update/keys.ts` ; `tools/signing/` (`keygen.mjs`, `sign.mjs`, clé de développement — **à remplacer par une clé de release hors dépôt en phase 11**). Détails protocole : doc 05 §9.

> **Implémentation (phase 11, 2026-08-22)** — `tools/release/` (pur Node, sans python) : `build.mjs` télécharge les distributions Node officielles **épinglées par sha256** (`constants.mjs`, version = `.node-version`) et shawl 1.9.0, et produit `release/<version>/` : `agent-<v>.js`, `mmo-agent-<v>-{win-x64.zip,linux-x64,linux-arm64,darwin-arm64}.tar.gz` (disposition = celle du launcher : `launcher.cjs`, `versions/<v>/agent.js`, `current.json`, `runtime/<node>/`, `runtime-current.json`, `manifest.json`, scripts d'installation, `shawl.exe` sous Windows) et `manifest.json` (sha256, tailles, signature Ed25519 du bundle, `signingKey`) ; archives **reproductibles** (tar/zip maison `archive.mjs`, entrées triées, mtime = `SOURCE_DATE_EPOCH`) ; `--panel` ajoute `mmo-panel-<v>-<hôte>` = runtime + `pnpm deploy --prod --legacy --config.node-linker=hoisted` du panel (modules natifs de l'hôte de build uniquement) + front + `dist-agent/` (les archives agent) + `mmo-panel.cmd`/`.sh`. `smoke.mjs` : sha256/contenu/modes de chaque archive + `launcher.cjs --version` réel sur la plateforme courante (CI : matrice 4 OS + job `release-archives`). **Signature** : `--key`/`MMO_SIGNING_KEY` (clé du mainteneur hors dépôt), `--release` refuse la clé de dev, la clé publique doit figurer dans `keys.ts` ; workflow `release.yml` sur tag `v*` (secret `MMO_SIGNING_KEY_PEM`). **Launcher** : `--version` (ou `pair`/`scan`) = exécution unique sans relance ni essai — seule évolution du launcher depuis la phase 9. **Amendement (2026-08-25, releases 1.0.2/1.0.3)** : `release.yml` passe en matrice 4 OS et produit aussi les **archives du panel** (win-x64, linux-x64, linux-arm64, darwin-arm64) — la dette « panel packagé pour l'hôte de build seulement » est levée ; les jobs Linux construisaient dans un **conteneur `ubuntu:20.04`** pour tenir un plancher glibc 2.31 (un `better-sqlite3` exigeant GLIBC_2.33 — constaté en réel sur une VM Ubuntu 20.04 ARM avec la 1.0.2 ; le conteneur seul n'a pas suffi, la 1.0.3 est sortie cassée pareil, et deux 1.0.4 ont raté avant que la recompilation ne prenne).
>
> **Amendement (2026-08-30) — la cause racine est traitée, pas contournée.** Le panel n'a plus de module natif SQLite (voir §3, `node:sqlite`). Trois conséquences :
> - **Le conteneur `ubuntu:20.04` et la recompilation de better-sqlite3 disparaissent de `release.yml` et de `build.mjs`.** Le seul `.node` encore embarqué est `@node-rs/argon2`, dont les prebuilds n'exigent que **GLIBC_2.14** (x64) et **2.17** (arm64) — mesuré sur les paquets npm, très en dessous du plancher.
> - **`build.mjs` vérifie ce qu'il archive** : il liste les `.node` du dossier déployé, lit le plancher de glibc de chacun directement dans l'ELF (les versions requises apparaissent en clair dans `.gnu.version_r` — aucune dépendance, aucun outil externe), **refuse d'archiver au-delà de `GLIBC_FLOOR` = 2.31** (`constants.mjs`), et charge réellement argon2 en faisant un hachage suivi de sa vérification.
> - **`smoke.mjs --panel` construit ET DÉMARRE l'archive du panel** : empreinte, contenu (dont les migrations Drizzle, oubliées une fois en déploiement manuel), puis lancement avec le runtime embarqué et attente de `GET /api/health` (version annoncée, driver SQLite, `mmo.db` créée et migrée). Branché dans `ci.yml` **sur chaque PR et sur les 4 OS**, plus un job qui démarre l'archive linux-x64 dans un conteneur **`debian:11`** (glibc 2.31, la configuration exacte de la VM qui a échoué). C'est le trou par lequel sont passées les archives cassées : ni la CI ni le smoke ne touchaient au panel. **Panel** : `DistributionService` (`<dataDir>/dist` ou `MMO_DIST_DIR`, manifeste relu à la volée) sert `GET /install.ps1`, `GET /install.sh` (templates `apps/panel/install/`, `__PANEL_URL__` ← `panel.publicUrl` sinon origine de la requête, `X-Forwarded-*` compris), `GET /dist/<fichier>` (fichiers listés par le manifeste seulement, `x-sha256`), `GET /api/dist` (statut + one-liners génériques), `GET /api/dist/:platform` (JSON plat lu par les scripts) ; dépôt admin `PUT /api/admin/dist/files/:file` + `PUT /api/admin/dist/manifest` (sha256/tailles vérifiés, **release d'agent publiée** avec la signature du manifeste), `DELETE /api/admin/dist` ; au démarrage, un manifeste présent (archive du panel) publie la release d'agent. `publish.mjs` : login + dépôt via l'API ou copie locale. **Scripts** : `install.sh` (POSIX sh ; Linux système = `/opt/mmo-agent`, `/var/lib/mmo-agent`, compte `mmo`, unit systemd `KillMode=process`/`Restart=on-failure`, `sudo` auto avec retéléchargement du script ; `--user-service` = `systemctl --user` + `loginctl enable-linger` sans root ; macOS = LaunchDaemon `AbandonProcessGroup`, `--user-service` = LaunchAgent ; `--archive` hors ligne, `--uninstall [--purge]`, versions reçues par `agent.update` conservées à la réinstallation) et `install.ps1` (PowerShell 5.1+ ; `%LOCALAPPDATA%\Programs\mmo-agent` + `%LOCALAPPDATA%\mmo-agent`, auto-élévation par copie du script et `Start-Process -Verb RunAs`, service **shawl** sous le **compte de l'utilisateur** (`Get-Credential`, `SeServiceLogonRight` accordé via `secedit`, compte posé par WMI sans mot de passe en ligne de commande) ou `-ServiceAccount LocalSystem`, `sc failure` restart, `-NoService`, `-Archive`, `-Uninstall [-Purge]`). Les deux scripts **appairent avant** d'installer le service via la nouvelle commande d'agent `pair --panel … --pair-code …` (`AgentConnection.pairOnly()`) : un code périmé échoue immédiatement et visiblement. **Amendement (2026-08-25, utilisation réelle)** : l'idempotence de `pairOnly()` est désormais **vérifiée par une sonde `auth.hello`** — une identité stockée n'est conservée que si CE panel l'accepte ; sur `E_AUTH` (état hérité d'un autre panel, machine retirée côté panel), l'agent se ré-appaire avec le code fourni et purge le journal d'événements de l'ancienne identité (avant : « déjà appairé » aveugle → rejet en boucle `unknown, unpaired or disabled agent` après réinstallation vers un nouveau panel). Une erreur réseau pendant la sonde reste fatale et visible (le code n'est pas consommé). **Testé** : archives des 4 plateformes construites et reproduites à l'identique (mêmes sha256), extraction par `tar`/`Expand-Archive` système, `launcher --version` Windows et Linux (WSL) ; `install.sh --user-service` de bout en bout dans WSL Ubuntu 22.04 + systemd (téléchargement, sha256, appairage, service actif et authentifié, linger, machine `online`, désinstallation) ; `install.ps1 -NoService` sous Windows PowerShell 5.1 (appairage réel, désinstallation `-Purge`) ; archive du panel win-x64 extraite et démarrée from scratch (front, `/api/dist` 4 plateformes, `install.ps1` servi). **Non rejoué** (pas d'élévation ni de machine vierge disponibles dans la session) : enregistrement effectif du service shawl sous compte utilisateur et reboot Windows, installation système Linux (`sudo`) et Linux ARM réel, launchd — à faire à la main avec le guide `docs/guide/installation.md` ; critère « les serveurs survivent à l'arrêt du service » à vérifier par OS à cette occasion.

> **Release 1.0.0 (phase 12, 2026-08-23)** : clé de release Ed25519 générée chez le mainteneur (privée hors dépôt, `~\.mmo\signing\`), clé publique ajoutée dans `apps/agent/src/update/keys.ts` (`RELEASE_KEYS`). **Durcissement** : `--release` pose `MMO_RELEASE_BUILD=1` pendant le build (esbuild `define`, entrée `env` de turbo.json) — le bundle publié n'accepte **que** les clés de release ; la clé de développement (privée dans le dépôt) n'est acceptée que par les builds de dev/test (`keys.test.ts`). Archives 1.0.0 des 4 plateformes + panel win-x64 signées avec la clé du mainteneur, `smoke.mjs` vert.

> **Test manuel `agent.update`/`runtime.update` réels (phase 12, 2026-08-24)** — chaîne complète exercée sur l'installation Windows de test : 0.11.0 → 0.12.0 (transition) → 1.0.0 (signature de release vérifiée par un agent réel), rollback réel (bundle 1.0.1 volontairement cassé → 2 crashs → `rolled_back`/`crash_loop`, serveur ré-adopté), `runtime.update` 24.19.0 → 24.18.0 → 24.19.0 (archives nodejs.org, sha256, `pending` puis bascule au redémarrage). Deux leçons actées :
> 1. **Amorçage de la rotation de clés** : un agent installé depuis un bundle de dev (n'embarquant que la clé de dev) **rejette** un bundle signé release (`E_SIGNATURE_INVALID`). Chemin opérationnel : publier d'abord un **bundle de transition signé dev** qui embarque les clés de release (`PUT /api/admin/agent-releases`), mettre à jour, puis pousser la release signée. Vaut pour toute rotation future : la nouvelle clé publique doit être livrée par un bundle accepté par l'ancienne.
> 2. **Course sur `update-result.json`** (corrigée) : le launcher écrit l'issue `applied` en réaction au `healthy` que l'agent envoie à la connexion — la lecture unique à la connexion la manquait donc systématiquement (remontée au redémarrage suivant), un `renameSync` EPERM concurrent a **crashé le launcher** (sauvé par shawl), et le `rm` du consume pouvait effacer sans le lire un résultat fraîchement réécrit. Correctifs : `writeJson` du launcher réessaie le rename (EPERM/EBUSY Windows) et `reportResult` ne propage plus jamais (`launcher.test.ts` : cible non remplaçable → launcher vivant, bascule actée) ; l'agent **revendique** le fichier par rename avant lecture et le relit à ~0/1/5 s après la connexion (`updateResultDelaysMs`, `agent.test.ts`).
>
> **Amendement (2026-08-30) — la revendication ne détruit plus rien avant d'avoir persisté.** Le correctif de session 3 laissait une fenêtre : la suppression du fichier revendiqué avait lieu dans un `finally`, donc **avant** le parse et avant toute émission. Toute mort du processus dans cet intervalle (SIGTERM du service, exit 75 d'une mise à jour enchaînée, SIGKILL au health-timeout) perdait l'issue **définitivement** — le nom revendiqué portait le PID, plus personne ne le relisait. C'est le défaut que masquait la tolérance `[flaky-ci]` du test phase9, désormais retirée. Nouvel ordre : **renommer → émettre → attendre la persistance → supprimer** (`claimUpdateResult()` / `releaseUpdateResult()`), nom revendiqué **fixe** (`update-result.claimed.json`) et **repris au démarrage suivant** s'il est resté orphelin, contenu invalide écarté immédiatement et journalisé. Trois corrections de fond l'accompagnent : `StateStore.flush()` attend désormais l'écriture **en vol** (sans quoi la durabilité de `stop()` était fictive) ; le fichier temporaire d'état est propre à l'instance (deux `StateStore` sur le même dossier écrasaient le même `.tmp`) ; les relectures différées sont **annulées par `stop()`** — un timer survivant consommait le fichier au nom d'une instance morte, poussait l'événement dans un état que plus personne ne relirait, et `trySend` abandonnait en silence (l'abandon est maintenant journalisé). Enfin le launcher écrit l'issue **avant** d'oublier l'essai : dans l'ordre inverse, une écriture qui échouait perdait les deux.

## 4. Gestionnaire Java intégré

- Mapping MC→Java : **manifest Mojang** (`piston-meta.mojang.com/.../version_manifest_v2.json`, champ `javaVersion.majorVersion`), caché côté panel ; table statique en fallback hors-ligne : `[1.12,1.17)→8` (**strictement 8** pour Forge ≤ 1.16), `[1.17,1.20.5)→17`, `[1.20.5,…)→21`. **Override par serveur** toujours possible.
- Téléchargement **multi-fournisseur** (matrice vérifiée le 2026-08-21) : **Temurin** (api.adoptium.net) → **Azul Zulu** (Java 8 macOS ARM, Java 17 Windows ARM) → **build x64 sous émulation** (Java 8 Windows ARM, introuvable ailleurs). Un 404 de l'API = combo indisponible (cas normal), passer au fallback.
- Chaîne de fallback décidée **par le panel** (payload de `java.install`) + **mode relais** : le panel télécharge et sert le JRE aux agents sans Internet sortant.
- Vérification SHA-256 systématique ; JREs sous `data/jre/<major>/`.

> **Implémentation (phase 9)** — amendement : les JRE gérés vivent sous `<stateDir>/java/<major>-<vendor>[-x64]/` (un dossier par fournisseur, donc Temurin et Zulu d'une même version peuvent coexister) ; `JavaRegistry` les énumère avec les JVM système. Chaîne de sources décidée par le panel (`@mmo/shared` `java/providers.ts` : URLs des API Adoptium/Azul, interprétation des réponses ; `services/java-runtimes.ts` : appels, mode relais avec cache `<dataDir>/jre-cache/`). Le sha256 est vérifié quand le fournisseur le publie (Temurin : toujours ; Zulu : détail du paquet) ; en relais, c'est le panel qui vérifie à la mise en cache et l'agent qui revérifie le fichier servi. L'extraction ne dépend d'aucun module : zip maison (`src/java/zip.ts`) et tar.gz via le tar de la phase 8 (modes POSIX conservés pour `bin/java`, liens symboliques internes au JRE créés).

## 5. Couche d'accès (pluggable — aucune API Tailscale dans le code)

Contexte réseau de référence : machine hôte en **IPv6 seul, IPv4 CGNAT, pas de redirection de ports** — mais les deux cas (traversée VPN et exposition directe) sont gérés.

| Mode | Mécanique | HTTPS | Pour qui |
|---|---|---|---|
| **`tailscale`** (défaut) | Tailnet privé ; panel derrière `tailscale serve` (écoute 127.0.0.1) | Cert Let's Encrypt auto pour `panel.<tailnet>.ts.net` | 100 % des amis (traverse CGNAT/4G/hôtel) ; plan gratuit = 6 utilisateurs |
| **`direct`** | Domaine perso + AAAA vers la box, pinholes pare-feu IPv6 | **ACME DNS-01 intégré au panel** + client DynDNS intégré (mise à jour de l'AAAA au changement de préfixe) | Amis avec IPv6 de bout en bout ; aucune app à installer |
| **`manual`** | Reverse-proxy/certificats fournis par l'utilisateur | À sa charge | Configurations avancées |

Faits vérifiés qui imposent ce design :

- PWA, service worker et Web Push exigent un **contexte sécurisé HTTPS** (seule exception : localhost) — même dans un tailnet.
- Le **push sort sur Internet** (FCM/Apple/Mozilla) : le panel a besoin d'Internet sortant pour notifier, quel que soit le mode.
- **iOS ≥ 16.4** : push uniquement pour la PWA **installée sur l'écran d'accueil** → onboarding guidé + re-synchronisation périodique des abonnements (iOS purge silencieusement).

**Jeu (connexion Minecraft des amis)** : réglage **par serveur** — `tailnet` ou `direct` — le panel affiche l'adresse exacte à donner aux amis pour chaque mode + test de joignabilité intégré. Minecraft Java gère l'IPv6 nativement.

Portabilité : si Tailscale change ses conditions → **Headscale** (serveur de contrôle open-source auto-hébergé, mêmes clients) ou mode `direct`, sans toucher au code MMO.

> **Lot 2 (2026-09-01) — une voie d'accès PAR MACHINE.** Le mode n'est plus exclusif : `access.direct.enabled` (`false` par défaut, Réglages → Accès) arme la voie directe **en plus** du mode courant — `AccessService.directEnabled()` (= mode `direct` OU réglage) remplace les gates `mode === 'direct'` de `start()`, `doIssue()`, `firewallRules()` et du bloc `direct` du statut ; le PATCH des réglages `access.*` **réapplique la couche à chaud** (`restart()` : timers + listener HTTPS, sans redémarrer le panel). Le statut expose `directEnabled` et `directUrl` (dérivée de `access.domain` + `access.httpsPort`). Côté machine : `machines.panel_url` (doc 04) = adresse de rattachement de l'agent, choisie **au moment de générer un code d'appairage** (modale de la page machine : Défaut / Voie directe / valeur en place) — `pairingDto` l'injecte dans les one-liners à la place de `panel.publicUrl`, et l'agent mémorise l'URL avec laquelle il s'est appairé (aucun changement agent, N−1 compris). `panel.publicUrl` reste LA valeur primaire pour le cookie `Secure`, le VAPID et le test de joignabilité ; le vocabulaire par-serveur `expose_mode` (joueurs) est distinct et inchangé.

> **Implémentation (phase 10, `apps/panel/src/services/access.ts` + `access/{acme,dns,der,mcping}.ts`)** — réglage `access.mode` (`tailscale` par défaut) et API `GET /api/access` (statut, tous rôles), `POST /api/access/test` (admin), `POST /api/access/certificate`, `POST /api/access/dyndns`, `GET /api/access/firewall`, `GET /api/servers/:id/address`, `POST /api/servers/:id/reachability`. Aucun appel à une API Tailscale : le mode `tailscale` se limite à afficher la commande `tailscale serve --bg --https=443 http://<host>:<port>` et à reconnaître passivement les en-têtes `Tailscale-User-Login/Name` posés par `tailscale serve` (`requestVia` : `tailscale` / `proxy` (X-Forwarded-*) / `direct`). **Test de joignabilité** (tous modes) : depuis l'hôte du panel, par l'URL publique — `GET /api/health`, sonde TLS (émetteur, validité), puis `/ws/probe` (endpoint WebSocket **public** qui annonce `via` et renvoie chaque frame telle quelle) avec une frame binaire de 64 KiB comparée octet à octet ; résultat persisté (`lastTest`) et affiché dans l'indicateur d'accès de l'en-tête. Testé en CI à travers un reverse-proxy qui relaie l'upgrade WebSocket (`access.test.ts`). **Mode `direct`** : client ACME RFC 8555 maison (compte ES256, JWS jwk→kid, nonce, `badNonce` rejoué, DNS-01 uniquement, CSR PKCS#10 produite par un encodeur DER minimal car `node:crypto` ne sait pas en générer, chaîne PEM téléchargée) ; fournisseurs DNS pluggables `access.dns.provider` = `manual` (le TXT `_acme-challenge.<domaine>` à poser est affiché dans les réglages, le panel sonde sa propagation jusqu'à 30 min) / `duckdns` (un appel pour TXT et AAAA) / `cloudflare` (API v4, zone déduite) / `generic` (URL de mise à jour `{ipv6}`/`{ipv4}`/`{host}`/`{token}`, DynDNS seulement) ; certificat et clé sous `<dataDir>/tls/` (`cert.pem`, `key.pem`, `account.json`, `state.json`), renouvellement quotidien si < 30 j (sauf `manual` : l'UI avertit) ; **listener HTTPS** créé à chaud après `listen` (`https.createServer` déléguant `request`/`upgrade` au serveur Fastify, `setSecureContext` au renouvellement), lié à `access.publicHost` ou à l'IPv6 globale « stable » détectée (jamais `::`), port `access.httpsPort` (443) ; **DynDNS** toutes les 10 min si `access.dyndns.enabled` (adresse publiée mémorisée, événement `access.addressPublished`, re-liaison du listener) ; **règles pare-feu** générées par OS (PowerShell `New-NetFirewallRule`, `ufw`/`firewall-cmd`, note pf) pour l'hôte du panel et chaque serveur `expose_mode = direct`, avec rappel des pinholes IPv6 de la box. Staging Let's Encrypt sélectionnable (`access.acme.directory`). Testé bout en bout contre un faux serveur ACME (JWS et CSR vérifiés, certificat signé par une CA de test) : HTTPS réel + WSS + frame binaire, rechargement au renouvellement. **Mode `manual`** : rien d'automatisé, seul le test s'applique. **Joueurs** : `GET /api/servers/:id/address` calcule « l'adresse à donner aux amis » selon `expose_mode` — tailnet : `machines.tailnet_host` > adresse 100.x / fd7a: remontée par l'agent (`machineInfo.addresses`, doc 05 §3) ; direct : `machines.public_host` > `access.domain` si la machine est l'hôte du panel (adresse globale commune) > IPv6 globale remontée — et `POST …/reachability` fait un **Server List Ping** (handshake + status, version/joueurs/MOTD) depuis l'hôte du panel. Non fait (volontaire) : accès depuis un téléphone hors réseau local dans les deux modes (pas de tailnet/domaine disponibles en CI — à jouer à la main avec la doc utilisateur de la phase 11), adresses IPv6 temporaires non distinguées des stables (heuristique « vue au tick précédent » + surcharge manuelle), renouvellement automatique en mode `manual`.

## 6. Sécurité (résumé)

- Panel : écoute uniquement sur 127.0.0.1 (mode tailscale) ou sur l'interface dédiée — jamais `0.0.0.0`. *Phase 10* : le listener HTTPS du mode `direct` se lie à l'adresse publique explicite (`access.publicHost`) ou à l'IPv6 globale détectée, jamais à `::` ; `/ws/probe` est public mais ne fait qu'un écho borné (15 s, taille de frame limitée par `maxPayload`) ; le jeton DNS (`access.dns.token`) est un secret d'`app_settings` jamais renvoyé par l'API (seul `access.dns.token.set` l'est).
- Sessions cookie (token haché en base), argon2id, RBAC admin/opérateur/lecture. *Phase 4* : cookie `mmo_session` (256 bits aléatoires, SHA-256 en base, `HttpOnly`, `SameSite=Lax`, `Secure` si `panel.publicUrl` est en https ou `MMO_COOKIE_SECURE=1`, 30 jours) ; **refus par défaut** — toute route non marquée `public` exige une session, rôle minimal par route (`viewer` < `operator` < `admin`) ; login limité à 10 essais/min par IP+compte ; le dernier admin actif ne peut être ni rétrogradé ni supprimé ; désactivation/changement de rôle/mot de passe = sessions révoquées. `/ws/client` authentifie par le même cookie à l'upgrade. *Phase 5* : la surface protégée est **`/api/*` et `/ws/*`** ; tout le reste (fichiers du front, fallback SPA) est public — le front ne contient aucun secret, les données passent toujours par l'API.
- Secrets d'agent : 256 bits, hachés côté panel, rotation/révocation. Codes d'appairage : hachés, TTL 15 min, 5 essais, rate-limit.
- L'authentification applicative des agents est **obligatoire même sous Tailscale** (les appareils des amis sont dans le tailnet ; Tailscale authentifie des machines, pas des rôles).
- **Port RCON** : bloqué hors machine locale (règle pare-feu posée par l'agent + ACL réseau documentée) — il écoute en clair sur toutes les interfaces.
- Bundles agent signés Ed25519 (§3).

> **Revue sécurité (phase 12, 2026-08-22)** — deux audits (surface HTTP/WS du panel ; accès fichiers de l'agent), menace retenue : anonyme sur un panel joignable, puis panel compromis / requêtes forgées sur le WS authentifié.
> **Corrigé** : (1) **contournement d'authentification** — le hook décidait sur l'URL brute alors que le routeur décode le pourcent-encodage (`GET /%61pi/settings` servait les réglages sans session) → décision sur le motif de route + URL décodée, déni au doute (`http/auth.ts` `isProtectedRequest`, test `api.test.ts`) ; (2) **brûlage des codes d'appairage** — 5 `pair.request` anonymes rendaient tout appairage impossible → limite **par adresse** (5 / 10 min, `RateLimiter`), plus d'incrément global ; (3) **messages internes en 500** (chemins, SQLite) → message générique, détail dans le journal ; (4) **en-têtes de sécurité** (`http/security.ts`) : CSP (`default-src 'self'`, styles inline Mantine, avatars `mc-heads.net`, `frame-ancestors 'none'`), `X-Frame-Options: DENY`, `nosniff`, `Referrer-Policy`, HSTS en HTTPS seulement ; (5) **injection dans les scripts d'installation** — `panel.publicUrl` et l'origine de la requête sont validés comme origine stricte `https?://hôte[:port]` (`util/origin.ts`) avant d'être insérés entre quotes dans `install.ps1/.sh` ; `requestOrigin()` passe par `request.protocol/host` (`trustProxy: loopback`) ; (6) `GET /api/dist/:platform` : `Object.hasOwn` (clé `constructor`) ; dépôt admin : tmp aléatoire nettoyé en `finally`. **Agent** : (7) **racines interdites** (`files/forbidden.ts`) — le dossier d'état et le dossier d'installation ne sont jamais acceptés comme chemin de serveur (jail `fs.*`), destination de sauvegarde (égal, contenu **ou contenant**), ni comme répertoire surveillé / chemin de scan (égal ou contenu) — un panel compromis pouvait faire réécrire `agent-state.json` ou `versions/<v>/agent.js` ; (8) `migration.finalize` ne renomme/purge que le dossier enregistré pour ce `serverId` ; (9) les manifestes de sauvegarde ne désignent plus un `archivePath` arbitraire (archive toujours à côté du manifeste → `backup.delete` ne supprime plus un fichier quelconque) ; (10) chemins **réservés** : corbeille (`.mmo-trash`, casse ignorée) et marqueur `.mmo-server.json` à la racine refusés comme cible de `write`/`mkdir`/`rename`/`copy`/upload/`fs.fetch` et comme source de `delete`/`rename` (un `rename` vers `.mmo-trash/0-x` valait suppression définitive à la purge) ; noms de périphériques Windows et segments à point/espace final refusés.
> **Accepté par conception (panel = autorité des machines)** : `config.javaPath`, `launch.jar`/argfile, `jvmArgs` et `java.install` (URL + sha256 du panel) exécutent ce que le panel demande ; `runtime.update` n'est pas signé (sha256 fourni par le panel) — un panel compromis a donc l'exécution de code sur ses machines ; la signature Ed25519 des bundles protège la **chaîne de distribution** (`agent.update`), pas contre le panel lui-même. `/api/health` public (versions, nombre d'agents). Cookie `SameSite=Lax` + JSON sans parser `form/multipart` = pas de CSRF exploitable ; pas de vérification d'`Origin` sur `/ws/client` (défense en profondeur possible).
> **Dette traitée (phase 12, 2026-08-23)** : `/ws/probe` borné (8 sondes simultanées → 1013, frame > 256 Kio ou > 4 Mio cumulés → 1009, 15 s) ; `/api/setup` : limiteur 5/min par adresse en `preValidation` (un corps invalide compte) + verrou en vol (`E_BUSY`), deux POST concurrents ne créent qu'un admin ; bornes de taille : `fs.fetch` 8 Gio (taille annoncée, `content-length`, puis flux réel ; `.part` supprimé) et **schéma http(s) seulement**, upload 64 Gio et jamais au-delà de la taille déclarée (`TransferReceiver` laisse passer la `ProtocolError` du consommateur), extractions tar/zip 64 Gio et 2 M d'entrées avec, pour le zip, un compteur sur le flux **inflaté** (en-tête menteur) — nouveau code `E_TOO_LARGE` (HTTP 413, non réessayable). Tests : `panel/http/hardening.test.ts`, `agent.backup.test.ts` (phase 12), `backup/tar.test.ts`, `java/zip.test.ts`.
> **Dette traitée (2026-08-30)** : toute clé de limiteur dérivée d'une adresse passe par `clientKey()` (`util/rate-limit.ts`) — IPv4 telle quelle, IPv6 **ramenée à son /64**, forme `::ffff:` traitée comme de l'IPv4 : un abonné IPv6 disposait sinon de 2^64 clés, donc d'aucune limite, et la carte du limiteur grossissait d'une entrée par tentative jusqu'à la purge en O(n). Le login gagne un **second limiteur par adresse seule** (30/min) en plus de `adresse|compte` (10/min), sans quoi faire varier le nom d'utilisateur suffisait à ne jamais être limité. Timing d'énumération : `users.authenticate` vérifie désormais le mot de passe contre un **hachage factice** (`dummyPasswordHash()`, calculé une fois) quand le compte est inconnu ou désactivé — les trois chemins d'échec coûtent le même temps.
>
> **Dette restante (actée pour 1.0)** : pas de filtre SSRF sur `fs.fetch` (URL locale/tailnet) ; `logs.search` avec regex non bornée (ReDoS) ; injection de `\n` dans les commandes générées par `config.set` live ; `/api/relay` migration sans compteur d'usage ; archives agent publiques sans limite de débit.

## 7. i18n et erreurs

- Tout texte visible passe par i18next (`fr`/`en`) dès la première ligne. Phase 2 : ressources en **modules TS typés** (`packages/shared/src/i18n/locales/{en,fr}.ts`, parité de clés vérifiée à la compilation par `satisfies` + test), espaces de noms `common` / `errors` (un texte par code protocole) / `detection` (sources et evidence) ; `createI18n(locale)` = instance isolée (le panel localise chaque push selon son destinataire), `resources` branché sur `react-i18next` côté web. *Phase 5* : les chaînes **d'interface** vivent dans l'espace de noms `web` côté `apps/web` (`src/i18n/locales/{en,fr}.ts`, même mécanisme `satisfies` + test de parité) et sont fusionnées avec `resources` de `@mmo/shared` (`common`/`errors`/`detection` restent partagés avec le panel pour les push). Langue initiale : préférence locale (`mmo.locale`) > langue du navigateur > `fr` ; après connexion, la langue du **compte** (`users.locale`) prime et tout changement depuis l'UI est persisté (`PATCH /api/auth/me`). Clés typées : `useT()` = `useTranslation` sur les quatre espaces (`web:…`, `common:…`, `errors:…`, `detection:…`).
- **Règle protocole** : `error.message` = anglais technique (logs) ; l'UI traduit à partir de `error.code` + `error.details`. Idem pour les textes de push (localisés par le panel selon la langue du destinataire).

## 8. Premier démarrage (bootstrap)

Wizard first-run : si la table `users` est vide → création du compte admin (endpoint verrouillé ensuite), génération des clés VAPID, choix du répertoire data et de la destination de backups par défaut, choix du mode d'accès. Rien de tout cela n'est fait « à la main ».

> **Implémentation (phase 4, API)** : `GET /api/setup/status` → `{ needsSetup }` ; `POST /api/setup { username, password, locale?, publicUrl?, accessMode?, backupDestination? }` crée l'admin, génère les clés VAPID (P-256 via `node:crypto`, privée jamais exposée par l'API), enregistre les réglages et ouvre la session ; `E_SETUP_DONE` ensuite. Tant qu'aucun utilisateur n'existe, les routes protégées répondent `E_AUTH` avec `details.setupRequired = true` (le front redirige vers le wizard). **Dérogation** : le répertoire data se choisit **par l'environnement** (`MMO_DATA_DIR`), pas dans le wizard — la base doit exister avant la première page.

## 9. Tests

| Niveau | Outil | Points clés |
|---|---|---|
| Unitaires | Vitest | mapping MC→Java, parsing logs, heuristiques de détection (fixtures copiées de vrais dossiers), schémas protocole, RBAC |
| Intégration panel | Vitest + `fastify.inject` + SQLite temporaire | API, auth, migrations rejouées from scratch |
| Intégration protocole | Panel + agent réels in-process | appairage, reconnexion, négociation de version, update + rollback |
| Intégration agent | **« fake Java server »** (script Node imitant un serveur MC : `Done`, joins, crash, RCON, délais) | start/stop/kill, watchdog, ré-adoption, garde-fou RAM — sans Java en CI |
| E2E | Playwright | flux PWA complets, viewports mobile + desktop, fr + en |
| Composants front | Vitest + jsdom + Testing Library | routeur + gardes + pages clés contre une API simulée (`fetch` stubbé), client temps réel avec faux WebSocket, projection WS → cache Query |
| CI | GitHub Actions : windows, ubuntu, **ubuntu-arm**, macos | rendue possible par le fake server |

> **CI réelle (post-1.0, 2026-08-25)** : première exécution à la publication du dépôt (aucun remote avant) — la matrice 4 OS + le job `release-archives` tournent et sont verts. Stabilisation en deux lots : (1) fragilités ponctuelles des tests (fixtures de logs commitées après exception au `.gitignore`, archives factices multi-OS, tolérances d'hôte via `it.runIf`) ; (2) remèdes **systémiques** de cadence, en un point : helpers `waitFor` ×3 sur CI + `testBudget()` pour les budgets d'`it`, `testTimeout` vitest 180 s (agent/panel) / 60 s (web) sur CI, `retry: 2` sur CI seulement — le local reste strict et rapide. Le retry a débusqué un vrai bug de harnais (plage RCON `[rconFrom, 65000]` vide quand le port éphémère dépasse 65000 → clamp 64 000). Deux tests restent tolérants-CI (`[flaky-ci]` : warn au lieu d'échec, stricts en local) : phase9 releases/rollback (Linux/ARM) et métriques RSS/TPS Windows — instabilités documentées, débogage à prévoir. Côté produit, la CI a fait émerger le disjoncteur CIM de `process-info` (doc 06 §1).

> **Implémentation (phase 5, e2e)** : `pnpm --filter @mmo/web e2e` — Playwright construit le front puis lance `e2e/fixtures/stack.ts` (`webServer`) : **panel réel** (sources `apps/panel/src`, SQLite temporaire, port `MMO_E2E_PORT` = 3999) servant le build, **agent réel** (sources `apps/agent/src`) dont Java est remplacé par le fake Java server, un serveur Vanilla minimal ; routes de pilotage `/e2e/*` (hors production). Projets : `setup` (wizard → machine → appairage → scan, une fois) puis `desktop-fr`, `desktop-en`, `mobile-fr` (Pixel 7), `mobile-en` en série (un seul serveur) : login → dashboard → start → console (snapshot, commande, réponse, historique ↑, complétion Tab) → joueurs → stop ; langue/thème persistés ; PWA (manifest, service worker actif, coquille hors ligne). Le texte de la console est vérifié via un **miroir textuel caché** (xterm rend sur une grille de cellules). **Lighthouse ≥ 12 n'a plus de catégorie PWA** : l'installabilité est vérifiée par les critères Chrome (manifest valide avec icônes 192/512, SW actif, hors ligne) dans `pwa.spec.ts`. *Phase 6* : `whitelist.spec.ts` (mêmes 4 projets) — le serveur e2e est en `online-mode=false` pour que la résolution d'UUID reste locale (aucun appel Mojang en test) ; le test lit `whitelist.json` et `server.properties` **sur disque** (chemin via `GET /e2e/info`) pour prouver que l'agent, puis le serveur, ont bien écrit les fichiers, et vérifie que seuls les onglets `players` ont été visités. Le Switch Mantine cache son `<input>` : en e2e on clique la piste (`label[for=id]`). *Phase 7* : `metrics.spec.ts` (mêmes 4 projets) — l'agent e2e échantillonne toutes les **1 s** (`metricsIntervalMs`), le test attend via l'API que `latest.players = 1` puis vérifie que la ligne « joueurs » est tracée (point arrivé par `/ws/client`), que le TPS indisponible est **dit** (vanilla 1.20.1 : aucune méthode honnête) et que le changement de plage annonce une autre résolution ; le `SegmentedControl` Mantine cache aussi son `<input>` (cliquer le `label`). *Phase 8* : `backups.spec.ts` (mêmes 4 projets) — backup à chaud depuis l'onglet Sauvegardes (task suivie en direct), archive lue **sur disque** et comparée au sha256 de l'API, téléchargement via le panel (transfert binaire, taille exacte), restauration en un clic (sécurité + relance), politique de backup (préréglage cron), action programmée ; chaque projet nettoie ce qu'il a créé via l'API (un seul serveur partagé).

## 10. Spikes de validation (réalisés le 2026-08-21 — voir [docs/spikes/](spikes/README.md))

1. **Comportement EOF stdin** — ✅ [confirmé](spikes/01-eof-stdin.md) : Vanilla 1.20.1, Forge 1.12.2/1.16.5, Fabric 1.21.1, NeoForge 1.21.1 survivent tous à la mort de l'agent et à l'EOF, restent pilotables par RCON et s'arrêtent proprement. Le modèle « serveurs détachés, stdin principal + RCON » est retenu tel quel ; la variante « java sans stdin, 100 % RCON » n'est pas nécessaire. Conditions : `detached: true` (Windows) et superviseur qui ne tue jamais l'arbre de processus (§3).
2. **Monitoring Windows 11 24H2+** — ✅/❗ [résultat](spikes/02-monitoring-windows.md) : `systeminformation` et `pidusage` n'utilisent plus `wmic` ; mais la comptabilité CPU par ticks est fausse sous Hyper-V → sidecar PowerShell + `QueryProcessCycleTime` (§1). Le fallback « CIM maison » initialement prévu aurait été faux lui aussi.
3. **zstd dans Node 24** — ✅ [validé](spikes/03-zstd-node24.md) : API streaming complète (Node ≥ 22.15), 5–9× plus rapide que gzip → zstd 3 par défaut, gzip en repli ; interdits/garde-fous en §1.

## 11. Risques assumés

| Risque | Mitigation |
|---|---|
| RSS agent ~100 Mo (vs ~30 en Go) | Sans conséquence sur les machines cibles ; bascule Go possible à coût borné |
| ~~`better-sqlite3` natif (panel uniquement)~~ | **Levé (2026-08-30)** : driver `node:sqlite`, plus aucun module natif SQLite ; garde-fou ESLint côté panel |
| Process Windows (pas de signaux POSIX) | Séquence stop→RCON→taskkill testée dédiément ; java lancé directement (jamais via shell/.bat) |
| Launcher = SPOF des mises à jour | Minuscule, figé, sur-testé, ne parle pas réseau |
| Push iOS capricieux | Onboarding guidé + resync ; fallback centre de notifications in-app (+ Discord futur). *Phase 10* : `apps/web/src/lib/push.ts` (support : HTTPS, service worker, PushManager, iOS ⇒ PWA installée sinon étapes « Partager → Sur l'écran d'accueil ») ; à chaque démarrage `resyncPush` renvoie l'abonnement courant (`last_seen_at`) ou le recrée si l'utilisateur l'avait activé (drapeau local `mmo.push`) ; le SW (`public/sw-push.js`, importé par le SW Workbox) gère `push`, `notificationclick` (focus/navigation vers l'URL de l'événement) et `pushsubscriptionchange`. Côté panel : `NotificationsService` — catégories `NOTIFICATION_TYPES` (protocole client, `notificationTypeOf(event)`), préférences `notification_prefs` avec défauts, livraison **localisée par destinataire** (`createI18n(locale)`, textes `common:notify.*`), purge d'un abonnement sur 404/410 ou 8 échecs, centre in-app = événements filtrés par préférences + curseur `users.notifications_seen_id`. |
| Churn de l'écosystème front | Versions épinglées ; protocole et agent indépendants des libs volatiles |
